# Vorgeschlagene Anpassungen

**Zweck:** Sammelt Architektur-Funde, die während der Codebase-Dokumentation (Meilenstein 3/4 der Demo-Vorbereitungs-Roadmap) auffallen, aber nicht sofort geprüft oder umgesetzt werden — um den Dokumentationsfluss nicht bei jedem Fund zu unterbrechen. Kein Ersatz für `features/INDEX.md` Known Issues (das trackt Feature-/Bug-Befunde aus Evals und manuellen Tests); hier stehen Architektur-/Vendor-/Design-Beobachtungen aus dem Lesen der Codebase selbst.

Jeder Eintrag muss durch die Codebase gegroundet sein (Datei:Zeile) — keine Vermutungen.

| # | Fundstelle | Befund | Vorschlag | Kategorie | Priorität | Status |
|---|---|---|---|---|---|---|
| 1 | [`voice-token/route.ts`](../../src/app/api/interview/[token]/voice-token/route.ts), [`.env.local.example:80-83`](../../.env.local.example) | Voice/STT läuft über ElevenLabs Scribe v2 Realtime als eigenständiges externes System (eigener API-Key, eigener Vendor), obwohl `INTERVIEW_MODEL` bereits durchgehend auf Google Gemini läuft. Gemini Live API unterstützt seit Native-Audio-Upgrade direkten Audio-Input inkl. Text-Transkript des Nutzer-Inputs (verifiziert via [ai.google.dev/gemini-api/docs/live-api](https://ai.google.dev/gemini-api/docs/live-api), Stand 2026-07). | Prüfen, ob Gemini Live API ElevenLabs für die Voice-Input-Funktion (PROJ-7) ersetzen kann — ein Vendor/API-Key weniger, potenzielle Kostenersparnis. Ungeklärt: ob die bestehende WebSocket-Realtime-Architektur (`hooks/useVoiceInput.ts`, `lib/audio/pcm-worklet.ts`) kompatibel mit Gemini Live API's Protokoll ist, oder ein Rewrite nötig wäre. | Vendor-Konsolidierung | — | offen — nicht technisch geprüft |
| 2 | [`interviews/route.ts`](../../src/app/api/interviews/route.ts), [`interviews/[id]/route.ts`](../../src/app/api/interviews/[id]/route.ts) (hartcodiert `.order('joined_at',{ascending:true}).limit(1)` — immer älteste Mitgliedschaft), [`use-cases/page.tsx`](../../src/app/dashboard/use-cases/page.tsx) + `roadmap/page.tsx` + [`process-steps/page.tsx`](../../src/app/dashboard/process-steps/page.tsx) (lesen `workspaceId` einmalig aus `user.user_metadata.workspace_id`), Migration [`20260520000005_proj10_workspace_members.sql`](../../supabase/migrations/20260520000005_proj10_workspace_members.sql) (`handle_new_user()` schreibt genau einen Workspace nach `user_metadata`) | Nutzeranforderung: Admin (Lias) muss zwischen mehreren Workspaces wechseln können (Dev-/Test-Workspace + je ein Workspace pro Kunde); Kundenseite (Projektverantwortliche(r)) soll perspektivisch eigenen Workspace-Zugriff bekommen, aber NICHT wechseln können. Ist-Stand-Prüfung (15 Routes mit `workspace_members`-Check durchgesehen): die Autorisierungsschicht ist bei 11 von 15 bereits pro Request gegen einen übergebenen `workspace_id` geprüft (`use-cases/*`, `process-steps/*`, `knowledge/search`, `interview/[token]/objects` — `use-cases/[id]/route.ts` lädt sogar alle Mitgliedschaften). Nur die zwei Interview-Listen-/Detail-Routes picken hartcodiert die älteste Mitgliedschaft. Der eigentliche Engpass ist ein anderer: es gibt nirgends eine Session-/UI-Vorstellung von "aktuell ausgewähltem Workspace" — alle drei Dashboard-Seiten lesen `workspaceId` aus dem bei Signup einmalig geschriebenen `user_metadata`-Skalar. | (1) Workspace-Switcher-UI + Session-State für "aktiver Workspace" statt statischem `user_metadata.workspace_id`; (2) die zwei hartcodierten Interview-Routes auf das param-basierte Muster der übrigen Routes umstellen; (3) Rollenkonzept für Admin nötig — ungeklärt ob (a) Admin wird in jedem Kunden-Workspace als `workspace_members`-Zeile eingetragen (nutzt bestehendes Modell, aber Bookkeeping pro Kunde) oder (b) echter Superuser-Bypass via neuem `is_admin`-Flag, das `workspace_members`-Checks umgeht. Design-Entscheidung (a) vs. (b) nötig vor Implementierung, z.B. via `/write-spec`. | Access Control / Multi-Tenancy | — | offen — Design-Frage (a)/(b) ungeklärt, kein PROJ-X-Eintrag |
| 3 | [`interviewAgent.ts:679-690`](../../src/services/interviewAgent.ts#L679) (`isReconnect`-Zweig in `createInterviewStream()`), [`reconnect/route.ts:98-133`](../../src/app/api/interview/[token]/reconnect/route.ts#L98) (Call+Import), zum Vergleich [`start/route.ts:86`](../../src/app/api/interview/[token]/start/route.ts#L86) (lebendiger `isStart`-Aufruf derselben Funktion). **Ergänzung 2026-07-13 (Interview-Engine-Deep-Dive):** derselbe tote `isReconnect`-Zweig existiert zusätzlich, unabhängig, in [`interviewTalker.ts:163-167`](../../src/services/interviewTalker.ts#L163) (`TalkerStreamOptions.isReconnect`) — auch dort grep-verifiziert: kein Aufrufer setzt dieses Feld je auf `true` (`createTalkerStream` wird nur von `runInterviewTurn.ts` ohne `isReconnect`/`isStart` und vom Eval-Runner mit `isStart:true` aufgerufen). | Nutzerfrage "kann Legacy-Code + `stepIdentity.ts` gelöscht werden?" geprüft — ursprüngliche Einschätzung (nur 1 Importeur von `stepIdentity.ts` → Legacy → löschbar) war **falsch**, importer-count allein war irreführend. `interviewAgent.ts` bündelt zwei Dinge: `buildTools()` (aktiv, Analyst+QuickExtract importieren es, `register_step`-Tool ruft `stepIdentity.ts` bei jeder Schritt-Registrierung auf — nicht löschbar) und `createInterviewStream()` (aktiv via `start/route.ts` bei JEDEM Interview-Kaltstart, tot via `reconnect/route.ts` — dort beweisbar unerreichbar, da `history` wegen atomarer Turn-Persistierung strukturell immer mit `role:'assistant'` endet, KI-22-Kommentar). Weder `interviewAgent.ts` noch `stepIdentity.ts` als Ganzes löschbar. | (A) Klein/sicher: toten `isReconnect`-Zweig + Call/Import in `reconnect/route.ts` entfernen (reine Ternary-Vereinfachung, keine Verhaltensänderung, da unerreichbar) — Scope jetzt zwei Fundstellen (`interviewAgent.ts` UND `interviewTalker.ts`). (B) Größer/eigene Entscheidung: sollte die `isStart`-Begrüßung langfristig über `runInterviewTurn.ts` (den ADR-016-Seam) statt über den separaten `createInterviewStream`-Prompt-Pfad laufen? Würde die bereits dokumentierte PROJ-37-Prompt-Drift (`STATIC_PROMPT` vs. `buildStaticPrompt()`) an der Wurzel lösen statt sie nur zu dokumentieren. **Nutzer-Entscheidung 2026-07-13: (A) nicht isoliert umsetzen — Verdacht, dass (B) das eigentlich sinnvolle Refactoring ist statt eines Flickens an Symptomen.** Beide Optionen bleiben als Vorschlag stehen, keine Umsetzung vor eigener Design-Runde. | Dead Code / Architektur-Konsolidierung | — | offen — bewusst nicht umgesetzt, wartet auf eigene Refactoring-Entscheidung (nicht Teil der laufenden Doku-Session) |
| 4 | [`interviewAgent.ts:47`](../../src/services/interviewAgent.ts#L47) (`findStepFuzzy`), [`interviewAgent.ts:61`](../../src/services/interviewAgent.ts#L61) (`findStepById`), zum Vergleich die aktiv genutzten, gleichnamigen Funktionen in [`turnStore/applyIntent.ts:59,76`](../../src/services/turnStore/applyIntent.ts#L59) | Interview-Engine-Deep-Dive (2026-07-13): beide lokalen Funktionen in `interviewAgent.ts` sind innerhalb der Datei nirgends aufgerufen (grep-verifiziert über den gesamten Import-Graph). Alle tatsächlichen Aufrufer (`session.stage()`/`applyIntent`, sowie extern `interviewQuickExtract.ts:22,95-98`) nutzen ausschließlich die gleichnamigen, unabhängig implementierten Versionen aus `turnStore/applyIntent.ts`. Vermutlich Überbleibsel aus der Zeit vor ADR-018 (Werkzeug-Schreibabsichten), als `buildTools()` diese Lookups noch selbst durchführte. | Beide toten Funktionen aus `interviewAgent.ts` entfernen (reine Löschung, keine Verhaltensänderung — Importe/Exporte anderer Dateien zeigen ausschließlich auf `applyIntent.ts`). | Dead Code | — | **umgesetzt 2026-07-14** — beide Funktionen gelöscht (AP1, Cleanup-Tranche); Endstand nach der vollen Tranche (AP1-AP12): 876/877 Tests grün |
| 5 | [`interviewOrchestrator.ts:107-110`](../../src/services/interviewOrchestrator.ts#L107) (`allStepsDone`), [`interviewOrchestrator.ts:132-134`](../../src/services/interviewOrchestrator.ts#L132) (`allMandatorySlotsFilled`) | Interview-Engine-Deep-Dive (2026-07-13): beide Funktionen sind definiert, nicht exportiert, und werden nirgends im Repo aufgerufen (grep-verifiziert). Vermutlich durch `semanticAllStepsDone` (L114) bzw. `computeMissingMandatorySlots` (aus `interviewSemantic.ts`) abgelöst, ohne die alten Versionen zu entfernen. | Beide toten Funktionen entfernen. | Dead Code | — | **umgesetzt 2026-07-14** — beide Funktionen gelöscht, jetzt-tote Imports (`computeMissingMandatorySlots`, `MANDATORY_SLOTS`) mitentfernt (AP1, Cleanup-Tranche) |
| 6 | [`app/api/interview/[token]/route.ts:46-50`](../../src/app/api/interview/[token]/route.ts#L46) (`.single()` bei `interview_state`, kein `checkTokenEndpointLimits`-Import), zum Vergleich [`start/route.ts:62`](../../src/app/api/interview/[token]/start/route.ts#L62) und [`reconnect/route.ts:61`](../../src/app/api/interview/[token]/reconnect/route.ts#L61) (beide `.maybeSingle()` + Rate-Limiting) | Interview-Engine-Deep-Dive (2026-07-13): der GET-Endpoint ist die einzige der 4 Token-Routes ohne Rate-Limiting (kein Import aus `@/lib/ratelimit`) — plausibel bewusst, da kein LLM-Call, aber ohne erklärenden Kommentar. Zusätzlich nutzt er `.single()` statt `.maybeSingle()` bei `interview_state`, inkonsistent zu den anderen beiden Routes; der `error`-Teil der Query wird nicht destrukturiert, wodurch das Verhalten bei fehlender Zeile praktisch identisch zu `.maybeSingle()` bleibt (funktional unkritisch, aber stilistisch inkonsistent). | Rate-Limiting ergänzen (Token+IP, analog zu den anderen 3 Routes) und `.maybeSingle()` für Konsistenz verwenden. | Inkonsistenz / Security-Hygiene | — | **umgesetzt 2026-07-14** — `checkTokenEndpointLimits`/`extractIP` ergänzt, `.single()`→`.maybeSingle()` (AP11, Cleanup-Tranche), `token.test.ts` entsprechend angepasst |
| 7 | [`talkerGroundingGuard.ts:160,188-194`](../../src/services/talkerGroundingGuard.ts#L160) (`_traceCtx`-Parameter nie in `experimental_telemetry` verdrahtet), zum Vergleich [`interviewTalker.ts:210`](../../src/services/interviewTalker.ts#L210) (`generate()`-Closure nutzt `buildTraceMetadata` aktiv) | Interview-Engine-Deep-Dive (2026-07-13), direkt KI-18-relevant: der Guard-Judge-Call in `checkGroundingViolation()` nimmt `_traceCtx` entgegen, übergibt es aber nie an `experimental_telemetry` — die Guard-Judge-Calls sind dadurch im Langfuse-Trace-Baum unsichtbar (nur `onTokenUsage`/Token-Zähler werden separat reportet). `features/INDEX.md` (KI-18) benennt als offenen nächsten Schritt explizit die Untersuchung der Guard-Sensitivität/Falsch-Negativ-Rate — ohne Trace-Daten ist das nur über manuelle Log-Analyse möglich. | `_traceCtx` tatsächlich in `experimental_telemetry` des Guard-`generateText`-Calls verdrahten (analog zum Talker-Muster), damit künftige Guard-Sensitivitäts-Analysen auf echten Trace-Daten aufsetzen können. | Observability-Gap (KI-18-Diagnose-Lead) | — | **umgesetzt 2026-07-14** — `checkGroundingViolation` verdrahtet `traceCtx` jetzt in `experimental_telemetry` (AP5, Cleanup-Tranche); Guard-Judge-Calls sind ab jetzt in Langfuse sichtbar, Guard-Sensitivitäts-Analyse selbst weiterhin offen (siehe KI-18) |
| 8 | [`runInterviewTurn.ts:227-263`](../../src/services/runInterviewTurn.ts#L227), insb. der `catch`-Block [`runInterviewTurn.ts:260-262`](../../src/services/runInterviewTurn.ts#L260) | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-12/KI-14-Fix: der synchrone Analyst-Rerun (eingeführt um stale Lifecycle-Entscheidungen zu verhindern) loggt einen eigenen Fehlschlag zwar strukturiert (`console.error`), degradiert danach aber still auf die ursprüngliche, potenziell stale Entscheidung — exakt das Problem, das der Rerun beheben sollte, bleibt im eigenen Fehlerfall unadressiert. Kein harter Fallback, keine Eskalation. | Diskussion, ob bei Rerun-Fehler ein harter Fallback (z.B. erzwungener `hard_stop` statt stiller Weiterlauf mit der alten Entscheidung) sinnvoller wäre — mindestens als bewusste Produktentscheidung dokumentieren, nicht nur als impliziten Nebeneffekt des `catch`-Blocks. | Residualrisiko hinter gelöster KI (KI-12/KI-14) | — | **umgesetzt 2026-07-14** — Nutzer-Entscheidung: Completion-Veto statt harter Fallback. `catch`-Block setzt jetzt `lifecycle = {shouldComplete:false, reason:null}` (AP9, Cleanup-Tranche); Turn läuft normal weiter, Analyst läuft in `background()` als natürlicher Retry. Neuer Test T13 in `runInterviewTurn.test.ts` |
| 9 | [`interviewOrchestrator.ts:198-212`](../../src/services/interviewOrchestrator.ts#L198) (`assertPhaseInvariants`), Konsum nur in [`interviewOrchestrator.ts:260`](../../src/services/interviewOrchestrator.ts#L260) (`walkthrough_step`-Case) | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-15-Fix: `assertPhaseInvariants` wird bei JEDEM `decideNextPhase`-Aufruf berechnet, das zurückgegebene `invariantViolated` aber nur im `walkthrough_step`-Case konsumiert — nicht in `coverage_check` (L281-284) oder `wrap_up` (L286-316), obwohl der Invariant selbst über "farewell approaching" spricht. Eine Regression, die sich erst in einer dieser zwei späteren Phasen zeigt, würde die Warnung nicht in einen Force-Transition ummünzen. **Korrektur (Cleanup-Tranche, 2026-07-14):** die `walkthrough_step`-Konsumption war selbst provably inert — in jeder Budget-Konfiguration liegt das Invariant-Fenster (`historyLength >= coverageCheckEscapeHL - 4`) zeitlich NACH `walkthroughEscapeHL`, sodass die Walkthrough-Escape-Valve (Z.268) und der KI-15-Unstarted-Step-Guard die Phase immer schon vorher weiterschieben, bevor dieses Fenster je erreicht wird — der Force-Transition-Zweig war folglich nie erreichbar, unabhängig von den zwei fehlenden Phasen. | Entweder `invariantViolated` auch in `coverage_check`/`wrap_up` konsumieren, oder explizit dokumentieren warum die Beschränkung auf `walkthrough_step` ausreicht. | Residualrisiko hinter gelöster KI (KI-15) | — | **umgesetzt 2026-07-14** — Nutzer-Entscheidung: da der Force-Transition-Zweig nachweislich inert war, auf reines Monitoring umgestellt statt auf mehr Phasen auszuweiten. `assertPhaseInvariants` gibt jetzt `void` zurück, loggt nur noch `console.warn` als Frühwarnsignal für Regressionen in der Escape-Valve-Reihenfolge (AP1, Cleanup-Tranche); Neutralitäts-Regressionstest ergänzt |
| 10 | [`reconnect/route.ts:80-85`](../../src/app/api/interview/[token]/reconnect/route.ts#L80) (409-Guard) und [`reconnect/route.ts:98-112`](../../src/app/api/interview/[token]/reconnect/route.ts#L98) (KI-22-Fix), keine gegenseitige Referenz zwischen beiden Stellen | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-22-Fix: die "history endet strukturell immer mit `role:'assistant'`"-Prämisse des Fixes (L100f) gilt nur, WEIL die ~30 Zeilen vorher liegende 409-Prüfung (`existingTurns.length===0`) den leere-History-Fall bereits ausschließt. Kein lokaler Kommentar an einer der beiden Stellen verweist auf diese Abhängigkeit — eine künftige Änderung an der 409-Prüfung könnte die KI-22-Fix-Prämisse still brechen, ohne dass der Zusammenhang beim Lesen einer der beiden Stellen isoliert auffällt. | Kommentar/Cross-Reference an beiden Stellen ergänzen (z.B. "Voraussetzung: siehe 409-Guard oben" bzw. "diese Prüfung ist Voraussetzung für die role==='assistant'-Prämisse weiter unten"). | Residualrisiko hinter gelöster KI (KI-22) | — | **umgesetzt 2026-07-14** — Cross-Reference-Kommentare an beiden Stellen ergänzt (AP7, Cleanup-Tranche), keine Verhaltensänderung |
| 11 | [`interviewAgent.ts`](../../src/services/interviewAgent.ts) (ganze Datei, 762 Zeilen) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, revidiert nach Nutzer-Rückfrage gegen zu simples "aufteilen"-Framing): `buildTools()` (Z.112-634) hat bereits heute ein einfaches, stabiles Interface (`buildTools(session, userInput?, opts?) → Tools`) und versteckt echte Komplexität dahinter — ein Deep Module im Sinne von `/codebase-design`, nur zufällig in derselben Datei wie das unabhängige, schrumpfende `createInterviewStream()` (Z.648-762, siehe Eintrag #3) untergebracht. Kein Größen-, sondern ein Zugehörigkeits-Problem: beide Teile teilen nur den Dateinamen. | `buildTools()` in ein eigenes Modul verschieben (z.B. `services/interviewTools.ts`) — das Interface ändert sich dabei NICHT, nur der Ort. `createInterviewStream()` bleibt vorerst separat oder wird im Zuge von Eintrag #3, Option (B) ohnehin obsolet. Reine Datei-Umstrukturierung, keine Verhaltensänderung. | Neu-Verdrahtung: Trennen | — | offen — nicht umgesetzt, wartet auf dieselbe Design-Runde wie #3 |
| 12 | [`interviewQuickExtract.ts:128-144`](../../src/services/interviewQuickExtract.ts#L128) (`record_slot`-Override per Objekt-Spread), zum Vergleich [`interviewAgent.ts:330-468`](../../src/services/interviewAgent.ts#L330) (`record_slot`-Basisdefinition in `buildTools()`) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, **zweite Revision** nach Nutzer-Rückfrage: "warum sollten `register_step`/`record_slot` allein aufgrund ihrer Größe ausgegliedert werden?"). Die erste Revision (s.u.) hatte "groß/komplex genug" als Kriterium akzeptiert — das war derselbe Fehler wie die ursprüngliche Fassung, nur an zwei statt sieben Werkzeugen: Größe allein ist kein Grund für eine neue Modulgrenze, entscheidend ist ein KONKRETER Grund (ein realer zweiter Konsument). Grep-Check: `register_step` hat keinen einzigen externen Verweis außerhalb der vollständigen `buildTools()`-Rückgabe — kein Grund für Trennung, bleibt unverändert in `buildTools()`. `record_slot` hat zwar einen echten zweiten Konsumenten (`interviewQuickExtract.ts` überschreibt `execute` per Objekt-Spread, muss signaturkompatibel bleiben, ohne dass ein Test das erzwingt) — aber eine Verschiebung in eine eigene Datei löst dieses Problem nicht, das Override-Muster selbst bliebe identisch fragil, nur an einem anderen Ort. Alle 7 Werkzeuge bleiben in `buildTools()` (dieselbe Kohäsions-Begründung wie bei `interviewAnalyst.ts`, siehe Eintrag zu #11 und Abschnitt 7.1). | Kein Datei-Split. Stattdessen den tatsächlichen Fund absichern: ein Test, der sicherstellt, dass `interviewQuickExtract.ts`s überschriebene `record_slot.execute`-Signatur mit `interviewAgent.ts`s Basisdefinition kompatibel bleibt (z.B. Typ-Assertion oder Snapshot-Test auf das `inputSchema`). Verhindert stilles Auseinanderlaufen bei künftigen Änderungen an einer der beiden Stellen. | Neu-Verdrahtung: Intern restrukturieren | — | **umgesetzt 2026-07-14** — neuer Test in `interviewQuickExtract.test.ts` validiert das Override-Payload gegen `record_slot.inputSchema` via `asSchema(...).validate()` (AP6, Cleanup-Tranche); per Sabotage-Test verifiziert, dass er bei echtem Schema-Drift tatsächlich rot wird |
| 13 | [`runInterviewTurn.ts:101-107`](../../src/services/runInterviewTurn.ts#L101), [`interviewTalker.ts:26-32`](../../src/services/interviewTalker.ts#L26), [`talkerGroundingGuard.ts:5-11`](../../src/services/talkerGroundingGuard.ts#L5) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): der `OnTokenUsage`-Callback-Typ ist strukturell identisch in allen drei Dateien unabhängig definiert — dreifache Duplikation statt eines geteilten Typs. Bei einer künftigen Erweiterung des Telemetrie-Contracts müssten alle drei Stellen synchron geändert werden, ohne dass ein Compiler-Fehler das erzwingt (strukturelle Typkompatibilität in TypeScript). Ziel ist WENIGER Definitionen (1 statt 3), nicht mehr Dateien. **Korrektur (Cleanup-Tranche, 2026-07-14):** grep-Vollprüfung vor Umsetzung fand zwei weitere, in der ursprünglichen Fundstelle nicht genannte Duplikate — `interviewAnalyst.ts` und `interviewQuickExtract.ts` definierten denselben Typ ebenfalls lokal. Tatsächlich **fünffache**, nicht dreifache Duplikation. | In eine gemeinsame Typdatei extrahieren (z.B. `services/telemetryTypes.ts`) und in allen drei Dateien importieren. | Neu-Verdrahtung: Zusammenführen | — | **umgesetzt 2026-07-14** — `OnTokenUsage` nach `_telemetry.ts` verschoben (bereits etabliertes Crosscutting Concept, kein neues Modul nötig), alle 5 Fundstellen importieren jetzt von dort (AP2, Cleanup-Tranche); Byte-Identität aller 5 Duplikate vor dem Merge verifiziert |
| 14 | [`talkerPrompt.ts:306-467`](../../src/services/talkerPrompt.ts#L306) (`buildDynamicContext`, Zeilen seit 2026-07-14 verschoben — ursprünglich 337-493) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, präzisiert): mit ~155 Zeilen und ~20 lokalen Sektionsvariablen (u.a. dicht verschachtelte Ternaries Z.357-406) ist dies die dichteste Einzelfunktion der Component außerhalb von `buildTools()`. Andere Sektionen der Datei sind bereits als benannte Funktionen ausgelagert (`formatStepTracker`, `formatExtractionsLog`, `formatFilledSlotsSnapshot`) — `buildDynamicContext` selbst folgt diesem Muster für seine eigenen ~20 Sektionen nicht. **Kein Datei-Split** — `talkerPrompt.ts` bleibt eine Datei, nur diese eine Funktion wird intern klarer strukturiert. **Korrektur (Prompt-Reduktions-Plan, 2026-07-14):** `formatExtractionsLog` und `formatFilledSlotsSnapshot` existieren nicht mehr (WP4/WP2 von #19 — beide waren reine Redundanzen, keine Auslagerung, jetzt gelöscht). `buildDynamicContext` selbst ist auf ~140 Zeilen/~15 Sektionen geschrumpft (vier Sektionen entfallen, ein neuer Early-Return-Zweig für `isCompletionFarewell` kam hinzu) — der ursprüngliche Befund (fehlende interne Struktur der verbleibenden Sektionen) bleibt unverändert bestehen, nur als kleinere Funktion. | Die verbleibenden Inline-Sektionsbauer (`focusLine`, `timingWarning`, `coverageCheckSection` u.a.) als eigene benannte Funktionen INNERHALB von `talkerPrompt.ts` extrahieren, analog zum bereits ausgelagerten `formatStepTracker`. `buildDynamicContext` bliebe reine Komposition benannter Aufrufe, keine neue Datei/kein neues Modul. | Neu-Verdrahtung: intern restrukturieren | — | offen — nicht umgesetzt |
| 15 | [`runInterviewTurn.ts:307-314`](../../src/services/runInterviewTurn.ts#L307) und [`runInterviewTurn.ts:490-495`](../../src/services/runInterviewTurn.ts#L490) (zwei `background`-Closures) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): beide Closures prüfen unabhängig voneinander denselben `preCompletionAnalystResult`-Zustand, um Doppel-Verarbeitung zu verhindern — dieselbe Logik zweimal statt in einem gemeinsamen Helper. Muss bei künftigen Änderungen an der Dedup-Bedingung synchron in beiden Closures nachgezogen werden. Ziel ist eine Stelle weniger, nicht mehr. | Gemeinsamen Helper (z.B. `alreadyProcessedByAnalyst(...)`) extrahieren, von beiden Closures aufgerufen. | Neu-Verdrahtung: Zusammenführen | — | **gestrichen 2026-07-14** — bewusst aus dem Cleanup-Tranche-Scope gestrichen (Plan-Review): die beiden Closures gehören zu unterschiedlichen Zweigen (Farewell-Pfad vs. Normalpfad) mit unterschiedlichem Rest-Kontext; ein gemeinsamer Helper hätte mehr Parameter-Fläche gebraucht als die aktuelle Duplikation an Risiko darstellt. Keine Umsetzung geplant |
| 16 | [`interviewOrchestrator.ts:304-314`](../../src/services/interviewOrchestrator.ts#L304) (`wrap_up`-Case in `decideNextPhase`) und [`interviewOrchestrator.ts:408-425`](../../src/services/interviewOrchestrator.ts#L408) (Trigger B in `checkLifecycle`) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): beide Stellen prüfen unabhängig voneinander dieselbe Kombination (`wrapUpQuestionAlreadyAsked` + "letzte Message ist user" + `clarification_cards`) — keine gemeinsame Helper-Funktion, muss synchron gehalten werden. Ziel ist eine Stelle weniger, nicht mehr. | Gemeinsame Prädikat-Funktion extrahieren (z.B. `readyForWrapUpCompletion(...)`), von beiden Stellen aufgerufen. | Neu-Verdrahtung: Zusammenführen | — | **umgesetzt 2026-07-14** — `wrapUpAnswerReceived(history)` extrahiert, von `decideNextPhase`s `wrap_up`-Case und `checkLifecycle`s Trigger B aufgerufen (AP8, Cleanup-Tranche); Cards-Handling bleibt an beiden Call-Sites lokal (unterschiedliche Ergebnistypen) |
| 17 | [`runInterviewTurn.ts:46-59,465-474`](../../src/services/runInterviewTurn.ts#L46) (`ExtractAndEmbedArgs`, Aufbau von `transcript`), [`extraction.ts:49-52,54-66`](../../src/services/extraction.ts#L49) (`buildExtractionPrompt`, `extractAndEmbed`) | Vertiefungs-Nachfrage zur Funktionstabelle (2026-07-13): `ExtractAndEmbedArgs.transcript` wird bei JEDEM Turn frisch aus der gesamten bisherigen Turn-Historie aufgebaut (`existingTurns.map(...)` + aktueller Turn, wächst O(n) mit der Interviewlänge) und an `extractAndEmbed` übergeben. Die tatsächliche Implementierung liest davon aber ausschließlich `transcript[transcript.length-1]` (`buildExtractionPrompt`, extraction.ts:49-52) — den letzten Eintrag. Der gesamte Rest des Arrays wird bei jedem einzelnen Turn neu konstruiert und sofort verworfen; kein Aufrufer im Repo braucht mehr als den letzten Turn (grep-verifiziert: nur ein Aufrufer, `runInterviewTurn.ts:469`). Zusätzlich ist `ExtractAndEmbedArgs` selbst als eigener, exportierter Typ deklariert, aber grep-verifiziert von keiner anderen Datei importiert (nur `RunTurnPorts` in derselben Datei referenziert ihn) — unnötige öffentliche Schnittstellenfläche. Echter, wiederkehrender Overhead (nicht nur Stilfrage): Datenkonstruktion, die mit der Interviewlänge wächst, ohne dass ihr Ergebnis je über den letzten Eintrag hinaus gebraucht wird. **Korrekturen (Cleanup-Tranche, 2026-07-14):** (1) `buildExtractionPrompt` liest vom letzten Transcript-Eintrag ausschließlich `user_input`, NIE `agent_response` — die Formulierung "liest transcript[transcript.length-1]" oben war ungenau, tatsächlich wird nur ein einzelnes Feld daraus gebraucht. (2) "nur ein Aufrufer" war falsch — `tsc --noEmit` nach der Typ-Verengung deckte zwei weitere Call-Sites mit demselben O(n²)-Transcript-Aufbau-Muster auf: [`app/api/interviews/[id]/reextract/route.ts`](../../src/app/api/interviews/[id]/reextract/route.ts) und [`scripts/reextract-interview.ts`](../../scripts/reextract-interview.ts) — insgesamt 3 Call-Sites mit demselben Muster, nicht 1. | (1) `ExtractAndEmbedArgs.transcript` von `Array<{user_input,agent_response}>` auf ein einzelnes `{user_input, agent_response}` (nur der letzte Turn) verengen — spiegelt exakt das, was gelesen wird, spart die wiederholte Array-Konstruktion bei jedem Turn. (2) `export` von `ExtractAndEmbedArgs` entfernen (nur intern in `RunTurnPorts` gebraucht), falls kein externer Konsument bewusst vorbereitet werden soll. | Overhead / unnötige Schnittstellenfläche | Niedrig | **umgesetzt 2026-07-14** — `transcript: Array<...>` → `userInput: string` verengt, `export` von `ExtractAndEmbedArgs` entfernt (AP4, Cleanup-Tranche); beide zusätzlich gefundenen O(n²)-Call-Sites (`reextract/route.ts`, `scripts/reextract-interview.ts`) auf O(1)-pro-Turn-Aufbau umgestellt |
| 18 | [`interviewTalker.ts:312-336`](../../src/services/interviewTalker.ts#L312) (Talker-Read-Merge-Write von `usedFillerPhrases` in `next_briefing`), [`interviewAnalyst.ts:69-78`](../../src/services/interviewAnalyst.ts#L69) (`AnalystBriefingSchema`, kein `usedFillerPhrases`-Feld) + [`interviewAnalyst.ts:463-478`](../../src/services/interviewAnalyst.ts#L463) (`produce_briefing`-Tool committet `capturedBriefing` als Ganzes), [`turnStore/applyIntent.ts:425`](../../src/services/turnStore/applyIntent.ts#L425), [`turnStore/supabaseTurnStore.ts:111-119`](../../src/services/turnStore/supabaseTurnStore.ts#L111) (`updateInterview`, vollständiger `.update(fields)` ohne JSONB-Merge) | Vertiefungs-Nachfrage zu `SEED_FILLERS`/`usedFillerPhrases` (2026-07-13): zwei unabhängige Schreiber auf dieselbe JSONB-Spalte `interviews.next_briefing`. Der Talker liest den aktuellen Stand frisch, merged und schreibt sorgfältig (Zeile 318-330: spread des bestehenden `next_briefing` + `usedFillerPhrases`-Update). Der Analyst committet dagegen bei JEDEM `produce_briefing`-Aufruf (laut Prompt "immer wenn eine substantielle State-Änderung stattfand", also den meisten inhaltlich relevanten Turns) das komplette `capturedBriefing`-Objekt als vollständigen Spalten-Ersatz — `AnalystBriefingSchema` (Zod) hat kein `usedFillerPhrases`-Feld, `capturedBriefing` kann es also nie enthalten, `updateInterview` schreibt `fields` roh ohne Merge. Läuft der asynchrone Analyst-Commit (via `after()`, typischerweise nach der Talker-Antwort) zeitlich nach dem Talker-Write, überschreibt er die akkumulierte Floskel-Liste vollständig. Der nächste Turn liest dann `persistedFillers.length===0` ([runInterviewTurn.ts:190-193](../../src/services/runInterviewTurn.ts#L190)) und fällt auf `SEED_FILLERS` zurück — die Floskel-Historie hält vermutlich selten länger als einen Turn durch. **Code-Pfad vollständig verifiziert (4 Stellen), aber noch nicht Laufzeit-beobachtet** — kein echter Multi-Turn-Log/DB-Dump in dieser Session geprüft. | Falls durch einen echten Mehrturn-Lauf bestätigt: `produce_briefing`-Commit sollte den bestehenden `usedFillerPhrases`-Wert aus dem aktuellen DB-Stand übernehmen statt ihn implizit zu droppen (z.B. beim Staging mit dem zuletzt gelesenen Wert mergen, nicht nur `capturedBriefing` roh schreiben). Nächster Schritt vor jeder Umsetzung: `next_briefing.usedFillerPhrases` über einen echten Mehrturn-Eval-Lauf beobachten, ob es tatsächlich wiederholt leer zurückfällt. | Funktionaler Bug-Verdacht (Floskel-Vermeidung vermutlich wirkungslos) | Mittel | **umgesetzt 2026-07-14, test-first** — RED-Test zuerst geschrieben (`applyIntent.test.ts`, bestätigt den Bug reproduzierbar: Patch enthielt kein `usedFillerPhrases`), dann gefixt: `TurnSnapshot` trägt jetzt `usedFillerPhrases` (geladen aus `interviews.next_briefing` in allen 3 Backends), `applyProduceBriefing` merged den Snapshot-Wert in den Patch zurück, wenn `intent.briefing` selbst keinen trägt (AP10, Cleanup-Tranche). Zusätzlicher Ende-zu-Ende-Test gegen echtes PGlite-SQL. Residualrisiko bewusst nicht vollständig gelöst: ein Same-Turn-Race mit dem Talker-eigenen Read-Merge-Write ist nur verengt, nicht eliminiert (kein Store-Level-Merge, keine DB-Migration — beide Ansätze laut Plan bewusst nicht gewählt) |
| 19 | [ADR-011](../adr/ADR-011-dual-loop-interview-architektur.md) D1/D2 (Zeile 46, 75-100, "< 500 Token dynamisch"), D3 (Zeile 140, "Talker hat null Tools"), D7 (Zeile 208-214, "Alle `Falsch: … Richtig: …` Blöcke entfernen. Alle `NIEMALS`, `VERBOTEN`, `PFLICHT` durch positive Formulierungen ersetzen", "Phase-Methodologie... reduziert sich auf 3-5 Zeilen"), Out-of-Scope-Zeile 341 ("Extraction-Service... läuft weiter parallel auf dem finalen Transcript") — geprüft gegen [`talkerPrompt.ts:31-85`](../../src/services/talkerPrompt.ts#L31) (`STATIC_PROMPT`), [`talkerPrompt.ts:190-287`](../../src/services/talkerPrompt.ts#L190) (`buildPhaseMethodology`), [`talkerPrompt.ts:337-493`](../../src/services/talkerPrompt.ts#L337) (`buildDynamicContext`, ~14 Sektionen), [`interviewTalker.ts:206`](../../src/services/interviewTalker.ts#L206) (`// NO TOOLS`), [`runInterviewTurn.ts:469`](../../src/services/runInterviewTurn.ts#L469) (`extractAndEmbed`-Aufruf pro Turn, nicht post-hoc) | Nutzer-Rückfrage (2026-07-13): "ist das nicht (i) zu viel Kontext, (ii) redundanter Kontext, (iii) Widerspruch zur Dual-Loop-Architektur?" — geprüft, alle drei bestätigt. **(iii) konkret:** `STATIC_PROMPT` enthält weiterhin einen wörtlichen `Falsch:.../Richtig:...`-Block (Zeile 45), einen eigenen `<verboten>`-Tag (Zeile 65-72) und `NIEMALS`/`VERBOTEN`/`PFLICHT` an mind. 8 Stellen (Zeile 42,43,61,62,65-66,272,402,453,464,479) — direkt entgegen D7. `buildPhaseMethodology` liegt je Phase bei 4-13 Zeilen (`walkthrough_step` mit 3 benannten Unter-Regeln E3.3/E3.5/E3.7) statt der zugesagten 3-5. Die Methodik-Texte referenzieren zudem Tool-Aufrufe (`record_slot`, `update_walkthrough_data`, `link_bottleneck` "aufrufen") für einen Talker, der laut Code korrekt NULL Tools gebunden hat (`// NO TOOLS — Talker is text-only (ADR-011 D3)`, `interviewTalker.ts:206`) — vermutlich unbereinigter Restbestand aus dem Methodik-Text des alten Single-Call-Agenten. **(ii) konkret:** ADR-011 erklärt PROJ-20/`extractAndEmbed` explizit zu Out-of-Scope mit der Prämisse "läuft parallel auf dem finalen Transcript" (also post-hoc) — der tatsächliche Code ruft `extractAndEmbed` aber nach JEDEM Turn auf und speist das Ergebnis (`extractionsLog`) live in denselben Talker-Prompt ein, der auch das Analyst-Briefing bekommt — zwei parallele, ADR-011 zufolge nie für Live-Interaktion vorgesehene "was wissen wir schon"-Kanäle. **(i) konkret:** kein gemessener Tokenwert (kein Tokenizer in dieser Session gelaufen), aber strukturell: `buildDynamicContext` komponiert ~14 mögliche Sektionen (Kontext-Kopf, unbegrenzte `extractionsLog`, Methodik, Step-Tracker, "bereits erfasst", Briefing, bis zu 6 Signal-Sektionen), mehrere davon typischerweise gleichzeitig aktiv — plausibel deutlich über der D1-Zielmarke, aber nicht exakt gemessen. **Einordnung, wie es entstand:** keine einzelne Fehlentscheidung, sondern viele einzeln gut begründete, eval-getriebene Ergänzungen über PROJ-22/23/29/30/31 hinweg (Drill-Stop, Ambiguität, Laddering, Floskel-Vermeidung, Anker-Sperre), von denen jede für sich einen dokumentierten Grund hat — aber nie gegen die ADR-011-D7-Kernthese (Over-Specification-Vermeidung bei fähigen Modellen) rückabgeglichen. | Zwei Optionen, keine Entscheidung getroffen: (A) Amendment zu ADR-011 (analog zu "Amendment A"), das die gewachsene Dichte bewusst akzeptiert und begründet, oder (B) eine gezielte Rückschlacke-Runde gegen die D7-Ziele (Falsch/Richtig-Block + `<verboten>`-Tag + PFLICHT-Marker entfernen, Methodik-Blöcke auf taktische 3-5 Zeilen kürzen, tote Tool-Referenzen aus der Methodik-Prosa entfernen, `extractAndEmbed`-Live-Einspeisung gegen die Out-of-Scope-Prämisse abgleichen). Erster Schritt vor beidem: echte Tokenzahl für (i) an einem realen Transkript messen, um die Einschätzung zu verifizieren statt nur strukturell zu argumentieren. | Architektur-Drift (ADR-011) | **Hoch** | **teilweise umgesetzt 2026-07-14** (Plan `curried-plotting-narwhal.md`, WP1-WP5) — Option B, aber bewusst NUR die strukturellen Redundanzen, nicht die D7-Sprachfrage selbst. Echte Tokenmessung nachgeholt (Langfuse, Interview `1f5d350d` Turn 31, 2026-07-11): Talker-Input 7714 Token (dynamischer Block davon 1562), Analyst-Input 9591-9776 Token — Analyst war schwerer als der Talker, obwohl D1 ihn nie adressierte. Umgesetzt: WP1 (Farewell-Turn unterdrückt fast den gesamten dynamischen Block, ~1562→~200-250 Token, löst nebenbei den unter Eintrag dokumentierten Live-Widerspruch zwischen `isCompletionFarewell` und `ambiguitySection`), WP2 (Dopplung "Bereits erfasste Werte" entfernt, ~210 Token/Turn außerhalb walkthrough_step), WP3 (Schritt-Tracker-Kompaktrendering für `[done]`-Steps, ~450 Token im gemessenen Sample), WP4 (siehe #22, `extractAndEmbed`-Live-Einspeisung entfernt), WP5 (Analyst-System-Prompt-Prefix-Stabilität, ADR-009-D2-Muster erstmals auf den Analyst angewendet, ~50% des Analyst-Inputs jetzt cache-eligible). **Weiterhin offen (bewusst nicht Teil dieser Revision):** die D7-Sprachfrage (Falsch/Richtig-Block, `<verboten>`-Tag, PFLICHT-Marker-Dichte) — zwei frühere Verdichtungsversuche an genau diesem Text haben `dialog_naturalness`-Regressionen ausgelöst (KI-18-Historie), bräuchte einen eigenen Live-Eval-Gate; siehe #26 (PFLICHT-Signal-Stapelung außerhalb der Farewell) als verwandtes offenes Folgethema. Tests: 5 neue Describe-Blöcke in `talkerPrompt.test.ts` + neue Datei `interviewAnalyst.test.ts`, 886/887 Tests grün. Eval-Verifikation für WP4/WP5 (Restrisiko-Check + `cacheReadTokens`): siehe Verifikations-Log unten. |
| 20 | [`extraction.ts:12-16`](../../src/services/extraction.ts#L12) (`RawExtraction`-Interface), [`interviewTypes.ts:13,26`](../../src/services/interviewTypes.ts#L13) (`import type` + `InterviewContext.extractionsLog`), [`talkerPrompt.ts:25`](../../src/services/talkerPrompt.ts#L25), [`runInterviewTurn.ts:41`](../../src/services/runInterviewTurn.ts#L41), [`start/route.ts:10`](../../src/app/api/interview/[token]/start/route.ts#L10), [`reconnect/route.ts:10`](../../src/app/api/interview/[token]/reconnect/route.ts#L10) (jeweils `import type`) | Nutzeranliegen (2026-07-14): die Interview-Gruppierung soll möglichst unabhängig von Prozessbasis sein (Pipeline-Prinzip: Interview führt Gespräch → Prozessbasis wertet aus → Use-Case-Engine leitet ab, ohne Rück-/Querkopplung). Prüfung des IE→PB-Import-Graphs (10 Datei-Kanten laut [`03-komponenten-uebersicht.md`](../03-komponenten-uebersicht.md)) grep-verifiziert Kante für Kante: 4 der 10 sind reine Typ-Importe, alle verursacht durch denselben Typ `RawExtraction` (definiert in extraction.ts, Prozessbasis), der als Feldtyp von `InterviewContext.extractionsLog` zentral im Interview-Kontext verbaut ist. Jede Datei, die den Kontext anfasst (talkerPrompt.ts, start/route.ts, reconnect/route.ts, runInterviewTurn.ts selbst), importiert dadurch zwangsläufig aus Prozessbasis, ohne dass eine der vier Stellen selbst Extraktions-Logik ausführt — reiner Compile-Zeit-Nebeneffekt der Typdeklaration, keine Laufzeit-Abhängigkeit. | `RawExtraction` aus extraction.ts nach `interviewSemantic.ts` (bereits geteiltes Typ-Fundament, siehe 01-woerterbuch.md) oder direkt nach `interviewTypes.ts` verschieben; extraction.ts importiert den Typ zurück. Streicht 4 der 10 IE→PB-Kanten, keine Verhaltensänderung. | Kopplungsreduktion (Interview↔Prozessbasis) | Niedrig | **umgesetzt 2026-07-14** — `RawExtraction` + `KnowledgeObjectType` nach `interviewSemantic.ts` verschoben, 13+ Importeure migriert (AP3, Cleanup-Tranche); madge-verifiziert |
| 21 | [`embeddings.ts:6`](../../src/services/embeddings.ts#L6) (`generateEmbedding`), [`processClustering.ts:9`](../../src/services/processClustering.ts#L9) (`cosineSim`), [`interviewAgent.ts:7`](../../src/services/interviewAgent.ts#L7) (Import `generateEmbedding`), [`stepIdentity.ts:16-17`](../../src/services/stepIdentity.ts#L16) (Import `cosineSim` + `generateEmbedding`) | Nutzeranliegen (2026-07-14), gleiche Prüfung wie #20: die restlichen 3 der 10 IE→PB-Kanten laufen von `interviewAgent.ts`/`stepIdentity.ts` in Prozessbasis-Dateien. Verifiziert ([`stepIdentity.ts:38-58`](../../src/services/stepIdentity.ts#L38), `classifyStepSimilarity`): der Vergleich läuft ausschließlich gegen den `stepTracker` DES LAUFENDEN Interviews (Live-Dedup "ist das derselbe Schritt wie vorhin genannt", z.B. beim `register_step`-Tool), nie gegen Prozessbasis' aggregierte, workspace-weite `process_steps`/`knowledge_objects`-Tabellen. Beide Funktionen sind reine Vektor-Mathe-/Embedding-API-Helfer ohne fachlichen Bezug zu Prozessbasis' eigentlicher Aufgabe (Cluster-Bildung über alle Interviews hinweg) — sie wohnen nur zufällig in Prozessbasis-Dateien, weil dort zuerst gebraucht. | `generateEmbedding` + `cosineSim` in einen neutralen, geteilten Ort verschieben (z.B. `lib/vectorUtils.ts`), analog zum bestehenden Sonderstatus von `_telemetry.ts`/`interviewSemantic.ts` als Crosscutting Concept. Streicht die restlichen 3 der 10 IE→PB-Kanten, keine Verhaltensänderung. ~~Nach #20+#21 zusammen: 0 Datei-Kanten Interview-Engine→Prozessbasis auf Code-Ebene.~~ **Korrektur (Cleanup-Tranche, 2026-07-14):** diese "0 Kanten"-Prognose war ungenau — madge fand nach Umsetzung von #20+#21 weiterhin 3 IE→PB-Kanten. Diagnose: die 3 verbleibenden Kanten sind nicht Typ-/Utility-Kopplung wie #20/#21, sondern die echte Verhaltens-Kopplung aus Eintrag **#22** (`defaultProdPorts()`s dynamische Importe) — bewusst außerhalb dieses Vorschlags, siehe #22. Richtige Bilanz: 7 von 10 IE→PB-Kanten eliminiert (#20: 4, #21: 3), 3 bleiben (#22, eigene Design-Entscheidung nötig). `cosineSim` zusätzlich in eine bestehende Datei verschoben (`embeddings.ts`, statt eines neuen `lib/vectorUtils.ts`) — `embeddings.ts` selbst wird durch diese Zuordnung zum Crosscutting Concept (analog `_telemetry.ts`/`interviewSemantic.ts`), da `generateEmbedding` bereits vendor-neutral von mehreren Components importiert wurde. | Kopplungsreduktion (Interview↔Prozessbasis) | Niedrig | **umgesetzt 2026-07-14** — `cosineSim` von `processClustering.ts` nach `embeddings.ts` verschoben (AP3, Cleanup-Tranche); `processClustering.test.ts` gelöscht (Inhalt vollständig nach `embeddings.test.ts` migriert, Nutzer-Freigabe eingeholt); `embeddings.ts` als Crosscutting Concept reklassifiziert (siehe 01-woerterbuch.md) |
| 22 | [`runInterviewTurn.ts:80-100`](../../src/services/runInterviewTurn.ts#L80) (`defaultProdPorts`, alle 3 dynamischen Importe), [`runInterviewTurn.ts:500`](../../src/services/runInterviewTurn.ts#L500) (`extractAndEmbed`-Aufruf im `onFinish`), [`runInterviewTurn.ts:489-493`](../../src/services/runInterviewTurn.ts#L489) (`runPostCompletionTasks` → `onCompleted`, Normalpfad), [`runInterviewTurn.ts:338-345,377-379`](../../src/services/runInterviewTurn.ts#L338) (`onCompleted`-Aufrufe in beiden `background`-Closures des Abschluss-Zweigs) | Nutzeranliegen (2026-07-14), Fortsetzung von #20/#21: die verbleibenden 3 der 10 IE→PB-Kanten sind keine Typ-/Utility-Kopplung wie #20/#21, sondern echte Verhaltens-Kopplung — alle 3 laufen aus `runInterviewTurn.ts`s eigenen Turn-Verarbeitungs-Closures (`onFinish`/`background`), nicht aus einem von Prozessbasis selbst kontrollierten Trigger. Zwei unterschiedliche Ursachen in dieser Gruppe: **(a)** `extraction.extractAndEmbed(...)`, aufgerufen bei JEDEM Turn (Zeile 500) — teilt die Ursache mit **Eintrag #19**: dort bereits als Beleg zitiert (`runInterviewTurn.ts:469`, mittlerweile 500), Option B von #19s Vorschlag nennt bereits "extractAndEmbed-Live-Einspeisung gegen die Out-of-Scope-Prämisse abgleichen". Löst sich #19 (Extraktion post-hoc statt live), verschwindet diese eine Kante als Nebeneffekt, keine separate Aktion nötig. **(b)** `enrichment.createProcessStepsFromTracker(...)` + `clustering.clusterProcessSteps(...)`, ausgelöst über `onCompleted` aus drei Stellen in `runInterviewTurn.ts` (Zeile 492 Normalpfad, Zeile 343/377 Abschluss-Pfad) — in #19 nicht erwähnt, eigenständiger Fund: die Ableitungs-Pipeline (Prozessschritte erzeugen, clustern, Wissensobjekte deduplizieren) wird synchron aus derselben Funktion ausgelöst, die den laufenden Dialog fährt, statt von einem Trigger, den Prozessbasis selbst kontrolliert. Use-Case-Engine zeigt bereits das Zielbild ([`03-komponenten-uebersicht.md:54`](../03-komponenten-uebersicht.md)): null Code-Kopplung zu Prozessbasis, reiner DB-Poll auf `process_steps`/`process_clusters`. | **(a)** keine separate Aktion, wird durch die Lösung von #19 mitgelöst. **(b)** Trigger-Verantwortung umkehren: Interview-Engines einzige Aufgabe bei Abschluss bleibt `interviews.status='completed'` setzen. Ob/wann Prozessbasis daraus ableitet, sollte über einen von Prozessbasis selbst kontrollierten Mechanismus laufen (Cron-Endpoint, Supabase DB-Trigger auf den Status-Wechsel, oder eine Queue), nicht über einen Funktionsaufruf aus `runInterviewTurn.ts`. `RunTurnPorts.onCompleted` entfiele dann komplett statt injiziert zu werden. | Kopplungsreduktion (Interview↔Prozessbasis) | Mittel | **(a) gelöst 2026-07-14** (WP4 von #19, Plan `curried-plotting-narwhal.md`) — `formatExtractionsLog(ctx.extractionsLog)`-Aufruf + "## Extrahierte Wissensobjekte"-Header aus `talkerPrompt.ts`s `buildDynamicContext()` entfernt (grep-verifiziert: einziger Rendering-Ort im gesamten Code). `extraction.extractAndEmbed()` selbst läuft unverändert weiter (Wissensbank-Pipeline, PROJ-4/PROJ-20) und speist weiterhin `computeDataSourcesBackfill()`, das die Talker-sichtbaren `hilfsmittel`-Slots über den korrekten, strukturierten Kanal befüllt — nur die zusätzliche Live-Rendering-Kante ist entfernt, die IE→PB-Kante selbst bleibt über die Typkopplung (`InterviewContext.extractionsLog: RawExtraction[]`) technisch bestehen, ist aber jetzt reine Speicher-/Threading-Plumbing ohne Prompt-Konsum. Eval-Verifikation des dokumentierten Restrisikos (schmales Zeitfenster vor struktureller Analyst-Zuordnung): siehe Verifikations-Log unten. **(b) weiterhin offen** — keine Design-Entscheidung, nicht umgesetzt. |
| 23 | [ADR-012](../adr/ADR-012-knowledge-graph-strategie.md) D2 (Zeile 70-104, neue Tabellen `knowledge_object_relations`+`use_case_sources`), D3 Phase 3-4 (Zeile 112-113, "Cluster-Aggregation"/"Cluster-multiplied ROI"), D4 (Zeile 118-127, Defer-Begründung + Revisit-Bedingung "≥10 abgeschlossene Interviews... prüfen ob Use Cases isoliert bleiben") — geprüft gegen [`processClustering.ts:92-139`](../../src/services/processClustering.ts#L92) (`synthesizeCluster`, LLM-Divergenz-Analyse) + [`useCaseEngine.ts:228-283`](../../src/services/useCaseEngine.ts#L228) (`sumParticipantRoi`/`sumParticipantHours`/`makeClusterUC`, Cluster-ROI-Aggregation) | Nutzer-Rückfrage zu ADR-012 (2026-07-14): ADR-012 (Deferred, 2026-05-30) begründet die vorgeschlagene neue Graph-Infrastruktur (D2) u.a. mit dem "Cross-Interview ROI-Multiplikator"-Problem (Zeile 36-39: "Process Cluster 'Rechnungsprüfung' mit 5 Mitarbeitern → ROI wird 5× unabhängig berechnet, niemals aggregiert"). Grep-Prüfung zeigt: genau dieses Problem ist bereits gelöst, ohne die in D2 vorgeschlagenen neuen Tabellen — `useCaseEngine.ts`s `sumParticipantRoi`/`sumParticipantHours` summieren ROI über alle Cluster-Teilnehmer, `processClustering.ts`s `synthesizeCluster` erzeugt zusätzlich eine LLM-Divergenz-Analyse pro Cluster. Beide nutzen ausschließlich die bereits vorhandene `process_clusters.participants`-JSONB-Spalte + `cluster_id`-FK, nicht `knowledge_object_relations`/`use_case_sources`. Vermutlich über PROJ-24 (Cluster-aware Use Case Generation, Deployed) geliefert. ADR-012s eigene Revisit-Bedingung (D4) ist damit im Kern erreicht, nur über einen anderen, leichteren Weg als D2 vorschlägt — der ADR-Status "Deferred" nennt diesen Zusammenhang nirgends. | ADR-012 einen Status-/Cross-Reference-Vermerk ergänzen (z.B. "Cross-Interview-ROI-Multiplikator, D2s Hauptmotivation, ist über PROJ-24 bereits ohne die hier vorgeschlagene Graph-Infrastruktur gelöst — verbleibender Scope von D2 nur noch: `knowledge_object_relations` für explizite Pain-Point/Tool-Kanten, `use_case_sources` für Provenance-Ketten im Dashboard"), damit ein künftiges Revisiting nicht mit einer veralteten Problem-Beschreibung startet. | Doku-Drift / veraltete Entscheidungsgrundlage | Niedrig | offen — nicht umgesetzt |
| 24 | [`extraction.ts:191-253`](../../src/services/extraction.ts#L191) (`deduplicateByType`, löscht Duplikat-Zeile + inkrementiert nur `existing_count`), insb. [Zeile 247-251](../../src/services/extraction.ts#L247) (Delete), zum Vergleich [`processClustering.ts:92-139`](../../src/services/processClustering.ts#L92) (`synthesizeCluster`, keine Löschung, LLM-Divergenz-Narrativ) + [`processClustering.ts:141-266`](../../src/services/processClustering.ts#L141) (`clusterProcessSteps`, verknüpft nur via `cluster_id`, keine Zeile geht verloren), zum Beleg in der UI [`ProcessStepsTable.tsx:262,324-337`](../../src/components/ProcessStepsTable.tsx#L262) | Nutzer-Rückfrage (2026-07-14): `pain_point`/`tool`-Dedup und `process_steps`-Clustering behandeln dasselbe fachliche Problem (dieselbe Sache, von unterschiedlichen Mitarbeitern unterschiedlich formuliert/mit abweichenden Details beschrieben) mit unterschiedlicher Reife. Bei `process_steps`: nicht-destruktiv, jede Mitarbeiter-Variante bleibt als eigene Zeile erhalten, zusätzlich generiert `synthesizeCluster` explizit eine "Gemeinsamkeiten/Abweichungen/Mögliche Ursachen"-Analyse (verifiziert in der UI). Bei `knowledge_objects`: destruktiv — sobald Cosine-Similarity über `DEDUP_THRESHOLD=0.92` und `isTextMatch` matchen, wird die "Duplikat"-Zeile gelöscht, nur ein Zähler (`existing_count`) steigt. Die individuelle Formulierung/Perspektive des gelöschten Mitarbeiters geht vollständig verloren, keine Narrativ-Erhaltung wie bei Prozessschritten. | Das bei `process_steps` bereits bewährte Muster (nicht-destruktiv verknüpfen + LLM-Divergenz-Narrativ statt löschen) auf `knowledge_objects` übertragen — z.B. `pain_point`/`tool`-Objekte über eine Kanten-Tabelle oder ein `group_id`-Feld verknüpfen statt zu löschen, mit einer analogen Synthese-Funktion für Fälle mit ≥2 Gruppenmitgliedern. Größerer Umbau als eine reine Schwellenwert-Anpassung, eigene Design-Runde nötig. | Datenverlust-Risiko / Inkonsistenz zwischen Prozessbasis-Teilfunktionen | Mittel | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 25 | [`extraction.ts:54-147`](../../src/services/extraction.ts#L54) (`extractAndEmbed`), insb. [Zeile 130-138](../../src/services/extraction.ts#L130) (Insert ohne vorherigen Vergleich gegen bereits existierende `knowledge_objects` desselben Interviews), zum Vergleich [`interviewAgent.ts:143-325`](../../src/services/interviewAgent.ts#L143) (`register_step`, 3-Schicht-Live-Dedup-Kaskade) | Nutzer-Rückfrage (2026-07-14), direkte Antwort auf "braucht die Interview-Engine Embeddings für Turn-zu-Turn-Unterscheidung neu/bereits-erfasst": für `pain_point`/`tool` existiert diese Fähigkeit aktuell GAR NICHT, unabhängig von Embeddings. `extractAndEmbed` fügt jede Extraktion unconditional ein, ohne Prüfung gegen bereits im selben Interview extrahierte Objekte. Der einzige Dedup-Mechanismus (`deduplicateKnowledgeObjects`) läuft ausschließlich post-hoc, nach Interview-Abschluss, workspace-weit — innerhalb eines laufenden Interviews kann derselbe Pain-Point/dasselbe Tool heute mehrfach in `knowledge_objects` landen, wenn der Mitarbeiter ihn in unterschiedlichen Turns unterschiedlich formuliert erwähnt. Im Gegensatz dazu hat `register_step` (Prozessschritte) einen live, turn-zu-turn wirksamen 3-Schicht-Mechanismus (exakt/Substring, Embedding-Similarity mit Hard/Soft-Zonen, Jaccard-Fallback). | Prüfen, ob ein analoger Live-Check für `extractAndEmbed` sinnvoll ist (z.B. vor dem Insert die Similarity gegen bereits im aktuellen Interview extrahierte `knowledge_objects` desselben `interview_id` prüfen, analog zu `classifyStepSimilarity`), oder ob die post-hoc-Dedup als bewusst ausreichend gilt, weil Pain-Points/Tools (anders als Prozessschritte) keine Orchestrierungs-/Lifecycle-Logik direkt beeinflussen und daher ein geringerer Blast-Radius vorliegt. Erster Schritt: prüfen wie oft dieser Fall in echten Läufen tatsächlich auftritt (Mehrfach-Erwähnung desselben Pain-Points/Tools innerhalb eines Interviews), bevor eine Live-Lösung gebaut wird. | Fehlende Fähigkeit (Interview-internes Dedup) | Mittel | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 26 | [`talkerPrompt.ts`](../../src/services/talkerPrompt.ts) — mehrere PFLICHT-Sections in `buildDynamicContext()` können gleichzeitig aktiv sein: `drillStopSection` (Zeile ~440), `ambiguitySection` (~452), `ladderiungSection`/LADDERING-ABBRUCH (~466), `stepTrackerSection`s "Slot-Target (PFLICHT)" (~388-391) — keine der Sections prüft, ob eine andere PFLICHT-Section im selben Turn bereits aktiv ist | Design-Runde #19 (2026-07-14, Plan `curried-plotting-narwhal.md`, TODO B): WP1 löst den PFLICHT-Konflikt NUR für den Sonderfall des skriptierten Farewell-Turns (`isCompletionFarewell`) — der allgemeine Fall (mehrere PFLICHT-Signale gleichzeitig in einem normalen, nicht-Farewell-Turn, z.B. Drill-Stop UND Ambiguität feuern im selben Turn) ist strukturell unverändert und nicht untersucht. Welches Signal hat Vorrang, wenn mehrere gleichzeitig feuern? Aktuell rein additiv (String-Konkatenation in Aufrufreihenfolge), kein Priorisierungsmechanismus. | Recherche: wie oft tritt Signal-Stapelung in echten Läufen tatsächlich auf (Log-/Transkript-Analyse über mehrere Eval-Batches), und falls relevant — Priorisierungsregel definieren (z.B. explizite Rangfolge oder ein einzelner "höchste-Priorität-PFLICHT-Section"-Mechanismus statt additiver Konkatenation). | Architektur-Drift (ADR-011) / Prompt-Konflikt-Risiko | — | offen — nicht umgesetzt, eigenständiges Recherche-Thema |
| 27 | [`interviewTalker.ts:309`](../../src/services/interviewTalker.ts#L309) (`getSupabaseAdmin()`-Direktaufruf für Filler-Phrase-Read-Merge-Write), zum Vergleich der Rest der Pipeline seit PROJ-34/ADR-018 (`TurnStore`/`ports`-Abstraktion, z.B. `runInterviewTurn.ts`, `interviewAnalyst.ts`, `interviewQuickExtract.ts`) | Design-Runde #19 (2026-07-14, Plan `curried-plotting-narwhal.md`, TODO C, Nebenfund beim Vollbild-Trace des Talker-Input-Pfads): `interviewTalker.ts` umgeht die `TurnStore`/`ports`-Abstraktion und ruft `getSupabaseAdmin()` direkt auf — ein Konsistenzbruch gegen das seit PROJ-34 konsequent durchgesetzte Muster, bei dem der Rest der Pipeline ausschließlich über `store`/`ports` persistiert (u.a. damit Eval-Läufe mit PGlite/Memory-Backends korrekt funktionieren). Aktuell harmlos: bei `--store pglite`-Evals matcht das Update 0 Zeilen in der echten Supabase-Tabelle (reiner No-op + zwei unnötige Roundtrips), kein Fehlverhalten, aber architektonisch inkonsistent. Nutzer-Entscheidung 2026-07-14: nur dokumentieren, kein Code-Fix in der laufenden Revision. | `interviewTalker.ts`s Filler-Phrase-Persistierung auf die `TurnStore`/`ports`-Abstraktion umstellen, analog zum Rest der Pipeline — macht das Verhalten auch unter `--store pglite`-Evals korrekt (aktuell still wirkungslos statt über den Adapter zu laufen) und beseitigt die letzte bekannte Ausnahme vom ADR-018-Muster. | Layering-Inkonsistenz (PROJ-34/ADR-018) | Niedrig | offen — bewusst nur dokumentiert, kein Fix (Nutzer-Entscheidung 2026-07-14) |

## Verifikations-Log: Prompt-Reduktions-Plan (WP1-WP5, 2026-07-14)

Eval-Batch nach Umsetzung von WP1-WP5 (Plan `curried-plotting-narwhal.md`, betrifft #19/#22a oben): `npm run eval:interview -- --models google/gemini-3.1-flash-lite --personas buchhalter --runs 3 --seed 42 --store pglite`. Reports unter `docs/evals/interview/2026-07-14/2026-07-14-2{1-14-26,1-20-14,1-26-56}-google-gemini-3-1-flash-lite-buchhalter-run{1,2,3}.md` + Aggregat `2026-07-14-21-26-56-...-aggregate.md`.

**Ergebnis: 2/3 PASS.** Aggregat-Median: `slotCoverage`/`dedupSlotCoverage` 0.93 (Gate ≥0.75), `dialogNaturalness` 0.67 (Gate ≥0.65, Min 0.33/Max 1 — normale Modell-Varianz), `hallucinationRate` 0/0/0 über alle 3 Läufe, `talkerGroundingViolations` 0/0/1 (KI-18-Baseline-Rauschen, kein neues Muster), `phaseAdherence`/`stepRegistrationCoverage` 1/1/1, `depth_score` Median 1.94.

**WP4-Restrisiko (dokumentiert oben: schmales Zeitfenster vor struktureller Analyst-Zuordnung) — nicht eingetreten:** `hallucinationRate` durchgehend 0, kein Lauf zeigt ein Muster, das auf fehlende Pain-Point-Sichtbarkeit vor der Analyst-Zuordnung hindeutet. Kein Anlass, WP4 zurückzunehmen oder nachzubessern.

**Run1-FAIL ist unabhängig von WP1-WP5:** `dedup_slot_coverage 0.67 < 0.75`-Gate verfehlt, weil die Persona im Mahnlauf-Schritt wiederholt echte quantitative Auskunft verweigerte ("kann ich Ihnen keine weiteren quantitativen Angaben... nennen", Turn 11/12 im Transkript) — dasselbe bereits dokumentierte Muster wie die historische KI-12/KI-15-Fail-Historie (Persona-Varianz bei einem spät im Gespräch behandelten dritten Prozess), keine neue Ursache. Alle 3 Läufe zeigen saubere, nicht-repetitive Verabschiedungen ohne erneut gestellte Fragen (KI-19-Fix durch WP1 intakt, z.B. Run1 letzter Turn: "Vielen Dank für deine Zeit und die wertvollen Einblicke... Ich wünsche dir noch einen produktiven Tag.").

**WP5 empirisch bestätigt — Gemini-Caching greift jetzt beim Analyst:** `analyst_online`-Komponente zeigt in der Kosten-Tabelle aller 3 Runs 57-70% Cache-Hit-Rate (Run1: 165.514/235.544 Input-Tokens = 70%, Run3: 145.068/256.099 = 57%), `analyst_catchup` 58% (Run3, einziger Lauf mit einem Catchup-Call). Vor WP5 war das architektonisch unmöglich (Prefix brach bei jedem Step-Tracker-Wechsel). Zum Vergleich: `talker`, `grounding_guard`, `quick_extract` zeigen weiterhin 0% Cache-Read über alle Runs — war nicht Teil des WP5-Scopes (Talkers `STATIC_PROMPT` ist seit ADR-009 D2 bereits stabil, aber Googles implizites Caching greift dort in dieser Stichprobe trotzdem nicht; eigenständige Beobachtung, kein Regressions-Befund dieser Revision, nicht weiter untersucht).

**Tests:** 886/887 Unit-Tests grün (1 Skip vorbestehend), `tsc --noEmit` sauber, vor und nach dem Eval-Batch.

