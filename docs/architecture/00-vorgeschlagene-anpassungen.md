# Vorgeschlagene Anpassungen

**Zweck:** Sammelt Architektur-Funde, die während der Codebase-Dokumentation (Meilenstein 3/4 der Demo-Vorbereitungs-Roadmap) auffallen, aber nicht sofort geprüft oder umgesetzt werden — um den Dokumentationsfluss nicht bei jedem Fund zu unterbrechen. Kein Ersatz für `features/INDEX.md` Known Issues (das trackt Feature-/Bug-Befunde aus Evals und manuellen Tests); hier stehen Architektur-/Vendor-/Design-Beobachtungen aus dem Lesen der Codebase selbst.

Jeder Eintrag muss durch die Codebase gegroundet sein (Datei:Zeile) — keine Vermutungen.

| # | Fundstelle | Befund | Vorschlag | Kategorie | Status |
|---|---|---|---|---|---|
| 1 | [`voice-token/route.ts`](../../src/app/api/interview/[token]/voice-token/route.ts), [`.env.local.example:80-83`](../../.env.local.example) | Voice/STT läuft über ElevenLabs Scribe v2 Realtime als eigenständiges externes System (eigener API-Key, eigener Vendor), obwohl `INTERVIEW_MODEL` bereits durchgehend auf Google Gemini läuft. Gemini Live API unterstützt seit Native-Audio-Upgrade direkten Audio-Input inkl. Text-Transkript des Nutzer-Inputs (verifiziert via [ai.google.dev/gemini-api/docs/live-api](https://ai.google.dev/gemini-api/docs/live-api), Stand 2026-07). | Prüfen, ob Gemini Live API ElevenLabs für die Voice-Input-Funktion (PROJ-7) ersetzen kann — ein Vendor/API-Key weniger, potenzielle Kostenersparnis. Ungeklärt: ob die bestehende WebSocket-Realtime-Architektur (`hooks/useVoiceInput.ts`, `lib/audio/pcm-worklet.ts`) kompatibel mit Gemini Live API's Protokoll ist, oder ein Rewrite nötig wäre. | Vendor-Konsolidierung | offen — nicht technisch geprüft |
| 2 | [`interviews/route.ts`](../../src/app/api/interviews/route.ts), [`interviews/[id]/route.ts`](../../src/app/api/interviews/[id]/route.ts) (hartcodiert `.order('joined_at',{ascending:true}).limit(1)` — immer älteste Mitgliedschaft), [`use-cases/page.tsx`](../../src/app/dashboard/use-cases/page.tsx) + `roadmap/page.tsx` + [`process-steps/page.tsx`](../../src/app/dashboard/process-steps/page.tsx) (lesen `workspaceId` einmalig aus `user.user_metadata.workspace_id`), Migration [`20260520000005_proj10_workspace_members.sql`](../../supabase/migrations/20260520000005_proj10_workspace_members.sql) (`handle_new_user()` schreibt genau einen Workspace nach `user_metadata`) | Nutzeranforderung: Admin (Lias) muss zwischen mehreren Workspaces wechseln können (Dev-/Test-Workspace + je ein Workspace pro Kunde); Kundenseite (Projektverantwortliche(r)) soll perspektivisch eigenen Workspace-Zugriff bekommen, aber NICHT wechseln können. Ist-Stand-Prüfung (15 Routes mit `workspace_members`-Check durchgesehen): die Autorisierungsschicht ist bei 11 von 15 bereits pro Request gegen einen übergebenen `workspace_id` geprüft (`use-cases/*`, `process-steps/*`, `knowledge/search`, `interview/[token]/objects` — `use-cases/[id]/route.ts` lädt sogar alle Mitgliedschaften). Nur die zwei Interview-Listen-/Detail-Routes picken hartcodiert die älteste Mitgliedschaft. Der eigentliche Engpass ist ein anderer: es gibt nirgends eine Session-/UI-Vorstellung von "aktuell ausgewähltem Workspace" — alle drei Dashboard-Seiten lesen `workspaceId` aus dem bei Signup einmalig geschriebenen `user_metadata`-Skalar. | (1) Workspace-Switcher-UI + Session-State für "aktiver Workspace" statt statischem `user_metadata.workspace_id`; (2) die zwei hartcodierten Interview-Routes auf das param-basierte Muster der übrigen Routes umstellen; (3) Rollenkonzept für Admin nötig — ungeklärt ob (a) Admin wird in jedem Kunden-Workspace als `workspace_members`-Zeile eingetragen (nutzt bestehendes Modell, aber Bookkeeping pro Kunde) oder (b) echter Superuser-Bypass via neuem `is_admin`-Flag, das `workspace_members`-Checks umgeht. Design-Entscheidung (a) vs. (b) nötig vor Implementierung, z.B. via `/write-spec`. | Access Control / Multi-Tenancy | offen — Design-Frage (a)/(b) ungeklärt, kein PROJ-X-Eintrag |
| 3 | [`interviewAgent.ts:679-690`](../../src/services/interviewAgent.ts#L679) (`isReconnect`-Zweig in `createInterviewStream()`), [`reconnect/route.ts:98-133`](../../src/app/api/interview/[token]/reconnect/route.ts#L98) (Call+Import), zum Vergleich [`start/route.ts:86`](../../src/app/api/interview/[token]/start/route.ts#L86) (lebendiger `isStart`-Aufruf derselben Funktion). **Ergänzung 2026-07-13 (Interview-Engine-Deep-Dive):** derselbe tote `isReconnect`-Zweig existiert zusätzlich, unabhängig, in [`interviewTalker.ts:163-167`](../../src/services/interviewTalker.ts#L163) (`TalkerStreamOptions.isReconnect`) — auch dort grep-verifiziert: kein Aufrufer setzt dieses Feld je auf `true` (`createTalkerStream` wird nur von `runInterviewTurn.ts` ohne `isReconnect`/`isStart` und vom Eval-Runner mit `isStart:true` aufgerufen). | Nutzerfrage "kann Legacy-Code + `stepIdentity.ts` gelöscht werden?" geprüft — ursprüngliche Einschätzung (nur 1 Importeur von `stepIdentity.ts` → Legacy → löschbar) war **falsch**, importer-count allein war irreführend. `interviewAgent.ts` bündelt zwei Dinge: `buildTools()` (aktiv, Analyst+QuickExtract importieren es, `register_step`-Tool ruft `stepIdentity.ts` bei jeder Schritt-Registrierung auf — nicht löschbar) und `createInterviewStream()` (aktiv via `start/route.ts` bei JEDEM Interview-Kaltstart, tot via `reconnect/route.ts` — dort beweisbar unerreichbar, da `history` wegen atomarer Turn-Persistierung strukturell immer mit `role:'assistant'` endet, KI-22-Kommentar). Weder `interviewAgent.ts` noch `stepIdentity.ts` als Ganzes löschbar. | (A) Klein/sicher: toten `isReconnect`-Zweig + Call/Import in `reconnect/route.ts` entfernen (reine Ternary-Vereinfachung, keine Verhaltensänderung, da unerreichbar) — Scope jetzt zwei Fundstellen (`interviewAgent.ts` UND `interviewTalker.ts`). (B) Größer/eigene Entscheidung: sollte die `isStart`-Begrüßung langfristig über `runInterviewTurn.ts` (den ADR-016-Seam) statt über den separaten `createInterviewStream`-Prompt-Pfad laufen? Würde die bereits dokumentierte PROJ-37-Prompt-Drift (`STATIC_PROMPT` vs. `buildStaticPrompt()`) an der Wurzel lösen statt sie nur zu dokumentieren. **Nutzer-Entscheidung 2026-07-13: (A) nicht isoliert umsetzen — Verdacht, dass (B) das eigentlich sinnvolle Refactoring ist statt eines Flickens an Symptomen.** Beide Optionen bleiben als Vorschlag stehen, keine Umsetzung vor eigener Design-Runde. | Dead Code / Architektur-Konsolidierung | offen — bewusst nicht umgesetzt, wartet auf eigene Refactoring-Entscheidung (nicht Teil der laufenden Doku-Session) |
| 4 | [`interviewAgent.ts:47`](../../src/services/interviewAgent.ts#L47) (`findStepFuzzy`), [`interviewAgent.ts:61`](../../src/services/interviewAgent.ts#L61) (`findStepById`), zum Vergleich die aktiv genutzten, gleichnamigen Funktionen in [`turnStore/applyIntent.ts:59,76`](../../src/services/turnStore/applyIntent.ts#L59) | Interview-Engine-Deep-Dive (2026-07-13): beide lokalen Funktionen in `interviewAgent.ts` sind innerhalb der Datei nirgends aufgerufen (grep-verifiziert über den gesamten Import-Graph). Alle tatsächlichen Aufrufer (`session.stage()`/`applyIntent`, sowie extern `interviewQuickExtract.ts:22,95-98`) nutzen ausschließlich die gleichnamigen, unabhängig implementierten Versionen aus `turnStore/applyIntent.ts`. Vermutlich Überbleibsel aus der Zeit vor ADR-018 (Werkzeug-Schreibabsichten), als `buildTools()` diese Lookups noch selbst durchführte. | Beide toten Funktionen aus `interviewAgent.ts` entfernen (reine Löschung, keine Verhaltensänderung — Importe/Exporte anderer Dateien zeigen ausschließlich auf `applyIntent.ts`). | Dead Code | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 5 | [`interviewOrchestrator.ts:107-110`](../../src/services/interviewOrchestrator.ts#L107) (`allStepsDone`), [`interviewOrchestrator.ts:132-134`](../../src/services/interviewOrchestrator.ts#L132) (`allMandatorySlotsFilled`) | Interview-Engine-Deep-Dive (2026-07-13): beide Funktionen sind definiert, nicht exportiert, und werden nirgends im Repo aufgerufen (grep-verifiziert). Vermutlich durch `semanticAllStepsDone` (L114) bzw. `computeMissingMandatorySlots` (aus `interviewSemantic.ts`) abgelöst, ohne die alten Versionen zu entfernen. | Beide toten Funktionen entfernen. | Dead Code | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 6 | [`app/api/interview/[token]/route.ts:46-50`](../../src/app/api/interview/[token]/route.ts#L46) (`.single()` bei `interview_state`, kein `checkTokenEndpointLimits`-Import), zum Vergleich [`start/route.ts:62`](../../src/app/api/interview/[token]/start/route.ts#L62) und [`reconnect/route.ts:61`](../../src/app/api/interview/[token]/reconnect/route.ts#L61) (beide `.maybeSingle()` + Rate-Limiting) | Interview-Engine-Deep-Dive (2026-07-13): der GET-Endpoint ist die einzige der 4 Token-Routes ohne Rate-Limiting (kein Import aus `@/lib/ratelimit`) — plausibel bewusst, da kein LLM-Call, aber ohne erklärenden Kommentar. Zusätzlich nutzt er `.single()` statt `.maybeSingle()` bei `interview_state`, inkonsistent zu den anderen beiden Routes; der `error`-Teil der Query wird nicht destrukturiert, wodurch das Verhalten bei fehlender Zeile praktisch identisch zu `.maybeSingle()` bleibt (funktional unkritisch, aber stilistisch inkonsistent). | Rate-Limiting ergänzen (Token+IP, analog zu den anderen 3 Routes) und `.maybeSingle()` für Konsistenz verwenden. | Inkonsistenz / Security-Hygiene | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 7 | [`talkerGroundingGuard.ts:160,188-194`](../../src/services/talkerGroundingGuard.ts#L160) (`_traceCtx`-Parameter nie in `experimental_telemetry` verdrahtet), zum Vergleich [`interviewTalker.ts:210`](../../src/services/interviewTalker.ts#L210) (`generate()`-Closure nutzt `buildTraceMetadata` aktiv) | Interview-Engine-Deep-Dive (2026-07-13), direkt KI-18-relevant: der Guard-Judge-Call in `checkGroundingViolation()` nimmt `_traceCtx` entgegen, übergibt es aber nie an `experimental_telemetry` — die Guard-Judge-Calls sind dadurch im Langfuse-Trace-Baum unsichtbar (nur `onTokenUsage`/Token-Zähler werden separat reportet). `features/INDEX.md` (KI-18) benennt als offenen nächsten Schritt explizit die Untersuchung der Guard-Sensitivität/Falsch-Negativ-Rate — ohne Trace-Daten ist das nur über manuelle Log-Analyse möglich. | `_traceCtx` tatsächlich in `experimental_telemetry` des Guard-`generateText`-Calls verdrahten (analog zum Talker-Muster), damit künftige Guard-Sensitivitäts-Analysen auf echten Trace-Daten aufsetzen können. | Observability-Gap (KI-18-Diagnose-Lead) | offen — nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 8 | [`runInterviewTurn.ts:227-263`](../../src/services/runInterviewTurn.ts#L227), insb. der `catch`-Block [`runInterviewTurn.ts:260-262`](../../src/services/runInterviewTurn.ts#L260) | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-12/KI-14-Fix: der synchrone Analyst-Rerun (eingeführt um stale Lifecycle-Entscheidungen zu verhindern) loggt einen eigenen Fehlschlag zwar strukturiert (`console.error`), degradiert danach aber still auf die ursprüngliche, potenziell stale Entscheidung — exakt das Problem, das der Rerun beheben sollte, bleibt im eigenen Fehlerfall unadressiert. Kein harter Fallback, keine Eskalation. | Diskussion, ob bei Rerun-Fehler ein harter Fallback (z.B. erzwungener `hard_stop` statt stiller Weiterlauf mit der alten Entscheidung) sinnvoller wäre — mindestens als bewusste Produktentscheidung dokumentieren, nicht nur als impliziten Nebeneffekt des `catch`-Blocks. | Residualrisiko hinter gelöster KI (KI-12/KI-14) | offen — Design-Frage, nicht umgesetzt |
| 9 | [`interviewOrchestrator.ts:198-212`](../../src/services/interviewOrchestrator.ts#L198) (`assertPhaseInvariants`), Konsum nur in [`interviewOrchestrator.ts:260`](../../src/services/interviewOrchestrator.ts#L260) (`walkthrough_step`-Case) | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-15-Fix: `assertPhaseInvariants` wird bei JEDEM `decideNextPhase`-Aufruf berechnet, das zurückgegebene `invariantViolated` aber nur im `walkthrough_step`-Case konsumiert — nicht in `coverage_check` (L281-284) oder `wrap_up` (L286-316), obwohl der Invariant selbst über "farewell approaching" spricht. Eine Regression, die sich erst in einer dieser zwei späteren Phasen zeigt, würde die Warnung nicht in einen Force-Transition ummünzen. | Entweder `invariantViolated` auch in `coverage_check`/`wrap_up` konsumieren, oder explizit dokumentieren warum die Beschränkung auf `walkthrough_step` ausreicht. | Residualrisiko hinter gelöster KI (KI-15) | offen — Design-Frage, nicht umgesetzt |
| 10 | [`reconnect/route.ts:80-85`](../../src/app/api/interview/[token]/reconnect/route.ts#L80) (409-Guard) und [`reconnect/route.ts:98-112`](../../src/app/api/interview/[token]/reconnect/route.ts#L98) (KI-22-Fix), keine gegenseitige Referenz zwischen beiden Stellen | Interview-Engine-Deep-Dive (2026-07-13), Residualrisiko hinter dem formal gelösten KI-22-Fix: die "history endet strukturell immer mit `role:'assistant'`"-Prämisse des Fixes (L100f) gilt nur, WEIL die ~30 Zeilen vorher liegende 409-Prüfung (`existingTurns.length===0`) den leere-History-Fall bereits ausschließt. Kein lokaler Kommentar an einer der beiden Stellen verweist auf diese Abhängigkeit — eine künftige Änderung an der 409-Prüfung könnte die KI-22-Fix-Prämisse still brechen, ohne dass der Zusammenhang beim Lesen einer der beiden Stellen isoliert auffällt. | Kommentar/Cross-Reference an beiden Stellen ergänzen (z.B. "Voraussetzung: siehe 409-Guard oben" bzw. "diese Prüfung ist Voraussetzung für die role==='assistant'-Prämisse weiter unten"). | Residualrisiko hinter gelöster KI (KI-22) | offen — kleine, sichere Doku-Ergänzung im Code, nicht umgesetzt (Doku-Session, keine `src/`-Edits ohne gesonderte Freigabe) |
| 11 | [`interviewAgent.ts`](../../src/services/interviewAgent.ts) (ganze Datei, 762 Zeilen) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, revidiert nach Nutzer-Rückfrage gegen zu simples "aufteilen"-Framing): `buildTools()` (Z.112-634) hat bereits heute ein einfaches, stabiles Interface (`buildTools(session, userInput?, opts?) → Tools`) und versteckt echte Komplexität dahinter — ein Deep Module im Sinne von `/codebase-design`, nur zufällig in derselben Datei wie das unabhängige, schrumpfende `createInterviewStream()` (Z.648-762, siehe Eintrag #3) untergebracht. Kein Größen-, sondern ein Zugehörigkeits-Problem: beide Teile teilen nur den Dateinamen. | `buildTools()` in ein eigenes Modul verschieben (z.B. `services/interviewTools.ts`) — das Interface ändert sich dabei NICHT, nur der Ort. `createInterviewStream()` bleibt vorerst separat oder wird im Zuge von Eintrag #3, Option (B) ohnehin obsolet. Reine Datei-Umstrukturierung, keine Verhaltensänderung. | Neu-Verdrahtung: Trennen | offen — nicht umgesetzt, wartet auf dieselbe Design-Runde wie #3 |
| 12 | [`interviewQuickExtract.ts:128-144`](../../src/services/interviewQuickExtract.ts#L128) (`record_slot`-Override per Objekt-Spread), zum Vergleich [`interviewAgent.ts:330-468`](../../src/services/interviewAgent.ts#L330) (`record_slot`-Basisdefinition in `buildTools()`) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, **zweite Revision** nach Nutzer-Rückfrage: "warum sollten `register_step`/`record_slot` allein aufgrund ihrer Größe ausgegliedert werden?"). Die erste Revision (s.u.) hatte "groß/komplex genug" als Kriterium akzeptiert — das war derselbe Fehler wie die ursprüngliche Fassung, nur an zwei statt sieben Werkzeugen: Größe allein ist kein Grund für eine neue Modulgrenze, entscheidend ist ein KONKRETER Grund (ein realer zweiter Konsument). Grep-Check: `register_step` hat keinen einzigen externen Verweis außerhalb der vollständigen `buildTools()`-Rückgabe — kein Grund für Trennung, bleibt unverändert in `buildTools()`. `record_slot` hat zwar einen echten zweiten Konsumenten (`interviewQuickExtract.ts` überschreibt `execute` per Objekt-Spread, muss signaturkompatibel bleiben, ohne dass ein Test das erzwingt) — aber eine Verschiebung in eine eigene Datei löst dieses Problem nicht, das Override-Muster selbst bliebe identisch fragil, nur an einem anderen Ort. Alle 7 Werkzeuge bleiben in `buildTools()` (dieselbe Kohäsions-Begründung wie bei `interviewAnalyst.ts`, siehe Eintrag zu #11 und Abschnitt 7.1). | Kein Datei-Split. Stattdessen den tatsächlichen Fund absichern: ein Test, der sicherstellt, dass `interviewQuickExtract.ts`s überschriebene `record_slot.execute`-Signatur mit `interviewAgent.ts`s Basisdefinition kompatibel bleibt (z.B. Typ-Assertion oder Snapshot-Test auf das `inputSchema`). Verhindert stilles Auseinanderlaufen bei künftigen Änderungen an einer der beiden Stellen. | Neu-Verdrahtung: Intern restrukturieren | offen — nicht umgesetzt |
| 13 | [`runInterviewTurn.ts:101-107`](../../src/services/runInterviewTurn.ts#L101), [`interviewTalker.ts:26-32`](../../src/services/interviewTalker.ts#L26), [`talkerGroundingGuard.ts:5-11`](../../src/services/talkerGroundingGuard.ts#L5) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): der `OnTokenUsage`-Callback-Typ ist strukturell identisch in allen drei Dateien unabhängig definiert — dreifache Duplikation statt eines geteilten Typs. Bei einer künftigen Erweiterung des Telemetrie-Contracts müssten alle drei Stellen synchron geändert werden, ohne dass ein Compiler-Fehler das erzwingt (strukturelle Typkompatibilität in TypeScript). Ziel ist WENIGER Definitionen (1 statt 3), nicht mehr Dateien. | In eine gemeinsame Typdatei extrahieren (z.B. `services/telemetryTypes.ts`) und in allen drei Dateien importieren. | Neu-Verdrahtung: Zusammenführen | offen — nicht umgesetzt |
| 14 | [`talkerPrompt.ts:337-493`](../../src/services/talkerPrompt.ts#L337) (`buildDynamicContext`) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13, präzisiert): mit ~155 Zeilen und ~20 lokalen Sektionsvariablen (u.a. dicht verschachtelte Ternaries Z.357-406) ist dies die dichteste Einzelfunktion der Component außerhalb von `buildTools()`. Andere Sektionen der Datei sind bereits als benannte Funktionen ausgelagert (`formatStepTracker`, `formatExtractionsLog`, `formatFilledSlotsSnapshot`) — `buildDynamicContext` selbst folgt diesem Muster für seine eigenen ~20 Sektionen nicht. **Kein Datei-Split** — `talkerPrompt.ts` bleibt eine Datei, nur diese eine Funktion wird intern klarer strukturiert. | Die verbleibenden Inline-Sektionsbauer (`focusLine`, `timingWarning`, `coverageCheckSection` u.a.) als eigene benannte Funktionen INNERHALB von `talkerPrompt.ts` extrahieren, analog zu den bereits ausgelagerten. `buildDynamicContext` bliebe reine Komposition benannter Aufrufe, keine neue Datei/kein neues Modul. | Neu-Verdrahtung: intern restrukturieren | offen — nicht umgesetzt |
| 15 | [`runInterviewTurn.ts:307-314`](../../src/services/runInterviewTurn.ts#L307) und [`runInterviewTurn.ts:490-495`](../../src/services/runInterviewTurn.ts#L490) (zwei `background`-Closures) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): beide Closures prüfen unabhängig voneinander denselben `preCompletionAnalystResult`-Zustand, um Doppel-Verarbeitung zu verhindern — dieselbe Logik zweimal statt in einem gemeinsamen Helper. Muss bei künftigen Änderungen an der Dedup-Bedingung synchron in beiden Closures nachgezogen werden. Ziel ist eine Stelle weniger, nicht mehr. | Gemeinsamen Helper (z.B. `alreadyProcessedByAnalyst(...)`) extrahieren, von beiden Closures aufgerufen. | Neu-Verdrahtung: Zusammenführen | offen — nicht umgesetzt |
| 16 | [`interviewOrchestrator.ts:304-314`](../../src/services/interviewOrchestrator.ts#L304) (`wrap_up`-Case in `decideNextPhase`) und [`interviewOrchestrator.ts:408-425`](../../src/services/interviewOrchestrator.ts#L408) (Trigger B in `checkLifecycle`) | Interview-Engine-Deep-Dive, Neu-Verdrahtungs-Bewertung (2026-07-13): beide Stellen prüfen unabhängig voneinander dieselbe Kombination (`wrapUpQuestionAlreadyAsked` + "letzte Message ist user" + `clarification_cards`) — keine gemeinsame Helper-Funktion, muss synchron gehalten werden. Ziel ist eine Stelle weniger, nicht mehr. | Gemeinsame Prädikat-Funktion extrahieren (z.B. `readyForWrapUpCompletion(...)`), von beiden Stellen aufgerufen. | Neu-Verdrahtung: Zusammenführen | offen — nicht umgesetzt |


