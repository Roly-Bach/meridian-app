# Known Issues — Historie (vollständig gelöst)

> Verlustfrei aus `features/INDEX.md` ausgelagert am 2026-07-15 (Thesis-Traceability, siehe [[project_thesis_dsr_framing]]). Ursprünglich dort als KI-1..17, 19, 20, 22 geführt. **Erweitert 2026-07-22** um KI-23, KI-24, KI-27 (code- und QA-verifiziert gelöst, siehe unten). Offene KIs (KI-18, KI-21, KI-25, KI-26, KI-28) bleiben in `features/INDEX.md`.

| ID | Severity | Beschreibung | Entdeckt in | Fix-Aufwand |
|----|----------|-------------|-------------|-------------|
| KI-1 | ✅ Resolved | ~~Bestehende `step_tracker`-Records haben string-kodierte Slots; PROJ-38 fix-forward, kein Backfill~~ → gelöst durch mains Read-Compat `parseJsonIfString` ([interviewSemantic.ts](../src/services/interviewSemantic.ts)), integriert im Merge 2026-06-21. Parst Altdaten beim Lesen, kein Backfill nötig. Lauf 2026-06-21: `slot_coverage 1.0` | Eval 2026-06-19 | — (gelöst 2026-06-21) |
| KI-2 | ✅ Resolved | ~~Knowledge-Object-Tool-Duplikation: mehrere Records pro distinktem Tool (19 Records / 4 Tools im buchhalter-Lauf)~~ → gelöst 2026-06-22: Root Cause war `deduplicateKnowledgeObjects()` in [extraction.ts](../src/services/extraction.ts) — hardcoded `.eq('type', 'pain_point')`, `tool`-Type wurde nie gededuped. Generalisiert auf alle `ALLOWED_TYPES` mit typ-spezifischem Text-Feld (`content.name` für tool, `content.description` für pain_point). 3 neue Unit-Tests, 630/631 Tests grün. **Follow-up 2026-06-23:** Eval-Lauf zeigte weiterhin Duplikate bei Namens-Varianten ("SAP FI" / "SAP FI-Modul") — der Match-Gate war ein absoluter `levenshtein > 8`-Cutoff, zu starr für längere Namen. Ersetzt durch `isTextMatch()` (Substring-Containment + längen-normalisiertes Lev-Ratio ≤0.4). Fängt Präfix-/Suffix-Drift unabhängig von Stringlänge. Fängt NICHT echte Synonyme ohne gemeinsamen Substring (z.B. "Finanzbuchhaltungssystem" vs. "SAP FI") — bräuchte Extraktions-Zeit-Kanonisierung, bewusst nicht gemacht (Restrisiko dokumentiert). 2 neue Unit-Tests. | Eval 2026-06-19 | — (gelöst 2026-06-22, Follow-up 2026-06-23) |
| KI-5 | ✅ Resolved | ~~`dialog_naturalness`-Gate `≥ 0.70` vs. Mapping {0.33/0.67/1.0}: nur Stufe 3 passiert~~ → gelöst durch mains PROJ-31 (Gate auf `≥ 0.65` gesenkt, [runner.ts](../src/services/__evals__/interview/runner.ts)), integriert im Merge 2026-06-21. Lauf 2026-06-21: `dialog_naturalness 0.67 ≥ 0.65` → **PASS** | Eval 2026-06-20 | — (gelöst 2026-06-21) |
| KI-6 | ✅ Resolved | ~~`/eval-interview`-Skill Schritt-4 (manuelle PASS-Kriterien) und das automatische Runner-Gate (`runner.ts`) divergieren: die manuellen Kriterien kennen die `dialog_naturalness`-Gate-Bedingung nicht (Gate jetzt `≥ 0.65`), daher kann ein Lauf nach Schritt 4 PASS, nach Runner-Gate FAIL sein~~ → gelöst 2026-06-22: Schritt 4 in [eval-interview/SKILL.md](../.claude/skills/eval-interview/SKILL.md) übernimmt jetzt den exakten `runner.ts`-Gate-Code (alle 5 Schwellenwerte) statt eigener manueller Kriterien; Hinweis ergänzt, den Block bei jedem Schwellenwert-Change in `runner.ts` nachzuziehen | Eval 2026-06-20 | — (gelöst 2026-06-22) |
| KI-7 | ✅ Resolved | ~~**Mitarbeiter-Interview-Link bricht für echte (nicht eingeloggte) Mitarbeiter:** `/interview/[token]` und `/api/interview/[token]/chat` werden von `src/middleware.ts` zu `/login` redirected~~ → gelöst 2026-06-22: `/interview/` und `/api/interview/` (mit Trailing-Slash, um `/api/interviews` plural NICHT mitzutreffen) zu `PUBLIC_ROUTES` ergänzt. Token-Auth läuft pro Route eigenständig (z.B. `chat/route.ts` prüft `access_token` + Expiry selbst), nicht über Supabase-Session — Middleware-Block war nie nötig. Curl-verifiziert in Dev + Production-Build, E2E-Regression grün. | QA-Regression PROJ-15/16, 2026-06-22 | — (gelöst 2026-06-22) |
| KI-8 | ✅ Resolved | ~~**Geschützte `/api/*`-Routes redirecten unauthentifiziert zu `/login` (307) statt 401 JSON zurückzugeben** — falscher Contract für programmatische Clients (z.B. Tests, künftige externe API-Konsumenten); via Redirect-Follow sah ein POST ohne Auth wie ein 200 aus~~ → gelöst 2026-06-22 in `src/middleware.ts`: Pfade unter `/api/` bekommen bei fehlender Session direkt `NextResponse.json({error:'Unauthorized'}, {status:401})` statt Redirect. Page-Routes verhalten sich unverändert (Redirect zu `/login`). | QA-Regression PROJ-15/16 (`tests/PROJ-3-interview-ui.spec.ts`), 2026-06-22 | — (gelöst 2026-06-22) |
| KI-9 | ✅ Resolved | ~~Talker-Halluzination wird von `hallucination_rate` nicht erfasst: Metrik greift nur auf Extraktions-/Schema-Ebene, nicht auf konversationelle Faktentreue. Zwei Reproduktionen 2026-06-22/23 (u.a. wortgleich zum in `talkerPrompt.ts` Z.43 dokumentierten Verbots-Beispiel "Du hast vorhin 1200 Minuten erwähnt"), `hallucination_rate = 0` trotzdem.~~ → 2026-06-23: neuer Scorer [talkerFactualGrounding.ts](../src/services/__evals__/interview/scorers/talkerFactualGrounding.ts), Cross-Vendor-Judge gegen komplettes Transkript. **Live-Verifikation 2026-06-24 deckte eigenen Bug auf:** erster 3-Lauf-Batch zeigte `talker_grounding_violations=2` in JEDEM Lauf (verdächtig konstant) — Ursache war die Transkript-Reihenfolge im Prompt selbst: `Agent (Turn N) → Mitarbeiter (Turn N)` statt der echten Kausalreihenfolge `Mitarbeiter → Agent` (gleiche Konvention wie `runner.ts` conversationLog). Der Judge sah die Agent-Zuschreibung VOR der Mitarbeiter-Aussage, der sie folgt — legitime Same-Turn-Referenzen sahen wie vorausschauende Erfindungen aus. Unit-Tests (Mock-Judge) fingen das nicht, da sie nur Parser-Logik prüfen, nicht den gebauten Prompt-Inhalt. Reihenfolge gefixt, zweiter 3-Lauf-Batch (selber Seed): `talker_grounding_violations` 1/0/1 statt 2/2/2 — verbleibende Treffer sind echte Befunde (z.B. Wrap-up-Frage referenziert nie genannte "letzte Arbeitswoche"). | Eval 2026-06-22, 2026-06-23, 2026-06-24 (Fix + 2× Live-Verifikation) | — (gelöst 2026-06-24) |
| KI-10 | ✅ Resolved | ~~`overwrite_churn` ist verfälscht: `computeTrailMetrics` ([runner.ts](../src/services/__evals__/interview/runner.ts) Z.262) filtert `source !== 'analyst'`, die echten Analyst-Labels heißen aber `analyst_online` / `analyst_catchup`. Dadurch zählen absichtliche Analyst-Verfeinerungen als Churn. Effekt: gemeldete churn ≈ 0.52–0.57, echter Quick-Extract-Churn nur ~0.02–0.04. Die PROJ-34-Neutralitäts-Baseline „≈0.38" basiert auf dieser Fehlzählung.~~ → gelöst 2026-06-23: Filter auf `!event.source?.startsWith('analyst')` geändert (matched jetzt `analyst`, `analyst_online`, `analyst_catchup`). 667 Tests grün (2 vorbestehende PGlite-Failures unverändert, kein Zusammenhang). Hinweis: nächster Eval-Lauf muss `overwrite_churn`-Baseline neu ziehen, „≈0.38" aus PROJ-34 ist obsolet. | Eval 2026-06-22 (PROJ-34) | — (gelöst 2026-06-23) |
| KI-11 | ✅ Resolved | ~~Eval-Lauf 2026-06-23 06:39 crashte auf Turn 24 mit `runInterviewTurn: interview ... not found`, im Eval-Report als „Supabase JWT-Clock-Skew" dokumentiert (gleiche Fehlerklasse wie frühere `JWT issued at future`-Vorfälle).~~ → Diagnose korrigiert 2026-06-23: Supabase Auth-Logs (`get_logs auth`, letzte 24h) zeigen **keinen** JWT-Fehler — die „JWT-Clock-Skew"-Theorie war eine unverifizierte Übernahme aus einem älteren Vorfall. Echte Ursache: [supabaseTurnStore.ts](../src/services/turnStore/supabaseTurnStore.ts) `loadInterview()` schluckte jeden Fetch-Error (inkl. transienter `TypeError: fetch failed`, gleiche Klasse wie die 3× Extraction-Fehler auf Turn 22) und gab `null` zurück — ununterscheidbar von einer echt fehlenden Zeile. [runInterviewTurn.ts:151](../src/services/runInterviewTurn.ts#L151) wertet `null` als "not found" und crasht hart. Fix: `withRetry()`-Helper (2 Retries, Backoff) in `loadInterview`, `insertTurn`, `completeInterview` — die drei Stellen auf dem kritischen Completion-Pfad. 3 neue Unit-Tests für `withRetry`, alle Tests grün. | Eval 2026-06-23 | — (gelöst 2026-06-23) |
| KI-13 | ✅ Resolved | ~~E2E-Suite zeigte 22 Fails, vermutet als CSP-Nonce-Hydration-Mismatch (Dev-Mode).~~ → Root Cause war ein anderer Bug: `playwright.config.ts` parst `.env.local` selbst (Zeilen 3–12) für den Test-Runner-Prozess, strippt aber keine umschließenden Quotes vom Value — `UPSTASH_REDIS_REST_URL="https://…upstash.io"` landete mit **literalen** Anführungszeichen im Env des von Playwright gespawnten `npm run dev`-Kindprozesses (Kind erbt Eltern-Env vor Next.js' eigenem `.env.local`-Loader, der bereits gesetzte Vars nicht überschreibt). Upstash-Client warf `UrlError` beim Init → `/api/interview/[token]/clarification` (und vermutlich weitere ratelimited Routes) lieferte 500 statt 404/400. Erklärte alle 10 deterministischen Chromium-Fails (`tests/PROJ-23-adaptive-clarification-questions.spec.ts`). Fix: Quote-Stripping (matching `"…"` oder `'…'`) im Parser ergänzt. Die ursprünglich vermutete CSP-Nonce-Hydration-Warnung (`<html nonce>` in Dev-Mode) **existiert weiterhin** als harmlose Konsolen-Warnung bei echten `page.goto()`-Navigationen (curl-/Build-verifiziert in PROJ-15 als produktionskorrekt, nur Dev-Mode betroffen) — verursacht aber keine Test-Fails, bewusst nicht weiter verfolgt (kosmetisch). **Verifikation:** `npm run test:e2e` mehrfach wiederholt — Chromium durchgehend 0 Fails; Mobile Safari zeigt bei jedem Lauf 0–2 Fails an **wechselnden** Tests (`PROJ-1`/`PROJ-3`/`PROJ-6`/`PROJ-24`, jeweils `waitForURL`/`ECONNRESET`-Timeouts) — bestätigt nicht-deterministisch (Webkit-Emulationsperformance), kein Code-Defekt, kein weiterer Fix-Bedarf. `npm test` 715/1 skipped unverändert, `tsc --noEmit` grün. | KI-13 geloggt 2026-06-25 (/deploy PROJ-36), Root Cause + Fix 2026-06-25 | — (gelöst 2026-06-25) |
| KI-12 | ✅ Resolved | ~~`dedup_slot_coverage`-Gate (`≥ 0.75`) ist der dominante reale FAIL-Grund über die Buchhalter-Lauf-Historie (10 Läufe 2026-06-22/23: 6 PASS / 3 FAIL / 1 PARTIAL). Wiederkehrendes Muster: dritter Prozess (meist spät entdeckt, z.B. Mahnlauf) bleibt bei `walkthrough` statt `done`.~~ → Root Cause 2026-06-24: [interviewOrchestrator.ts](../src/services/interviewOrchestrator.ts) `checkLifecycle()` entscheidet `soft_confirm` anhand des `stepTracker`-Snapshots vom START des Turns — der Analyst läuft im Dual-Loop (ADR-011) im Hintergrund und hat den GERADE eingegangenen `userInput` noch nicht gesehen. Fix: [runInterviewTurn.ts](../src/services/runInterviewTurn.ts) — bei `reason==='soft_confirm'` läuft `runAnalystOnline` jetzt synchron VOR der Completion-Entscheidung, Tracker neu geladen, `checkLifecycle` erneut aufgerufen; findet sich ein neu registrierter Schritt, wird Completion verworfen, Turn läuft normal weiter (kein Doppel-Run). 1 neuer Regressionstest (T11), 11/11 grün. **Live-verifiziert 2026-06-24** (`--models google/gemini-3.1-flash-lite --runs 3 --seed 42`, zwei Batches á 3 Läufe = 6 Läufe gesamt): `dedup_slot_coverage` durchgehend 0.89–0.93, **0 von 6 Läufen** zeigen das alte Muster (vorher 3 von 10 FAIL exakt an dieser Schwelle). 1 Lauf im zweiten Batch FAIL, aber an `blocked_rate=0.1` (Gate `<0.1`, Grenzfall) — unabhängig von KI-12, nicht weiter verfolgt. | Eval 2026-06-22, 2026-06-23 (Befund), 2026-06-24 (Fix + Live-Verifikation) | — (gelöst 2026-06-24) |
| KI-14 | ✅ Resolved | ~~Eval-Batch 2026-06-25 (buchhalter, gemini-3.1-flash-lite, seed 42, 3 Läufe): 2/3 FAIL auf echten Gates, nicht Judge-Rauschen — Run1 `overwrite_churn 0.32` (Gate `<0.20`) + `dialog_naturalness 0.33` (Gate `≥0.65`). Rohes `transcript.json` zeigte Turn 29: Agent-Text wortgleiches Echo des User-Inputs, nach 2× Goodbye (Turn 27/28) feuerte die volle Tool-Pipeline nochmal (`register_step` ×4 auf bereits registrierte Steps).~~ → Root Cause: die Farewell-Loop-Escape-Valve in [interviewOrchestrator.ts](../src/services/interviewOrchestrator.ts) `checkLifecycle()` (Zeile ~339, eingeführt für B6/PROJ-23) erkennt 2 konsekutive Abschieds-Marker, aber ihr Gate `analystSuggestion !== null` blockte komplett, wenn der Analyst zu dem Zeitpunkt noch keine Briefing geliefert hatte — der Turn fiel dann stillschweigend in eine normale Verarbeitung zurück, ohne dass Substanz zum Fragen da war; der Talker (gemini-3.1-flash-lite) degenerierte zum Echo. KI-12s Sync-Rerun griff hier nicht, weil er nur bei bereits `shouldComplete===true` triggert. Fix: neuer Reason-Wert `'farewell_pending_analyst'` — wenn Farewell-Marker matchen aber `analystSuggestion===null`, signalisiert `checkLifecycle` das statt durchzufallen; [runInterviewTurn.ts](../src/services/runInterviewTurn.ts) erzwingt darauf denselben synchronen Analyst-Rerun wie bei `soft_confirm` (KI-12-Pattern), bevor der Turn normal weiterläuft. 2 neue/erweiterte Tests (T12 in `runInterviewTurn.test.ts`, Assertion-Erweiterung in `interviewOrchestrator.test.ts`), 716/717 grün. **Live-verifiziert 2026-06-26** (`--personas buchhalter --runs 3 --seed 42 --store pglite`, zwei Batches): kein Verbatim-Echo mehr in keinem Run — Farewell-Loop wird jetzt korrekt durch eine echte Rückfrage statt Echo aufgelöst. Pass-Rate insgesamt unverändert (weiterhin 2/3 FAIL je Batch), aber aus anderen, bereits dokumentierten Ursachen (Modell-Varianz: `talker_grounding_violations`, `hallucination_rate`, genuine redundante `record_slot`-Calls) — kein Hinweis auf Rückfall des Echo-Bugs. | Eval 2026-06-25 (Befund), 2026-06-26 (Fix + Live-Verifikation) | — (gelöst 2026-06-26) |
| KI-15 | ✅ Resolved | ~~Eval-Batch 2026-06-26 (buchhalter, gemini-3.1-flash-lite, seed 42, KI-14-Verifikation): `dedupSlotCoverage`-Median fiel 0.92 (06-24/25) → 0.72, 2/3 FAIL neu an diesem Gate (`≥0.75`). Alle 3 Läufe zeigten dasselbe Muster: die Wrap-up-Probe ("etwas Wiederkehrendes, das wir noch nicht erwähnt haben?") deckte einen späten Prozess auf (z.B. Mahnwesen), der als `exploring`-Step mit **0/9 Slots** registriert wurde, wonach das Interview sofort endete — der leere Step inflierte nur den Denominator.~~ → Root Cause: KI-14s Farewell-Escape-Valve in [interviewOrchestrator.ts](../src/services/interviewOrchestrator.ts) `checkLifecycle()` vervollständigte bei `analystSuggestion!==null ∧ keine clarification_cards` — ohne den Uncovered-Step-Guard, den Trigger B (wrap_up-Pfad) bereits hatte. Ein gerade vom synchronen Analyst-Rerun (KI-12/KI-14-Pattern) registrierter, noch unbearbeiteter Prozess wurde dadurch übersprungen statt exploriert. Fix: neuer privater Helper `hasUnstartedExploringStep()` (Status `exploring` ∧ alle Potenzial-/Tazite-Slots null ∧ keine Walkthrough-Daten) blockt die Farewell-Valve gezielt nur für diesen Fall — bewusst enger als Trigger B's Guard, da B6 (Test `completes even when Mahnprozess is walkthrough`) bereits etabliert hatte, dass ein incomplete-aber-gestarteter Walkthrough-Step die Valve NICHT blockieren darf. Gebunden an dasselbe Turn-Budget (`coverageCheckEscapeHL`) wie der bestehende wrap_up-Escape-Valve, damit eine späte Erwähnung das Interview nicht unbegrenzt streckt. `decideNextPhase`s Clarification-Routing (Pt8) blieb unverändert — bestätigt durch [interviewAnalyst.ts:192-197](../src/services/interviewAnalyst.ts), dass der Analyst pro leerem Pflicht-Slot eine SlotCard generiert, auch für einen inhaltlich komplett leeren Step; der Bug war ausschließlich die vorzeitige Completion, nicht die Routing-Wahl. 3 neue Tests in `interviewOrchestrator.test.ts` (unstarted-Step blockt, gestarteter Step blockt nicht — B6 unverändert, Budget-Bound greift), 730/731 grün. **Live-verifiziert 2026-06-27** (`--personas buchhalter --runs 3 --seed 42 --store pglite`): `dedupSlotCoverage` 0.89/0.89/0.96 (Median 0.89, über Vor-Regressions-Baseline 0.92-Range), alle 3 Steps erreichen `done` (kein dangling leerer Step mehr), **3/3 PASS** — erster vollständig grüner Batch seit Beginn der Eval-Historie. | Eval 2026-06-26 (Befund), 2026-06-27 (Fix + Live-Verifikation) | — (gelöst 2026-06-27) |
| KI-16 | ✅ Resolved | ~~Eval-Batch 2026-06-26 (buchhalter run3): `hallucination_rate 0.17` (Gate `<0.01`) trotz inhaltlich korrekter Extraktion. Stichprobe zeigte: evidence_quote im Slot-Trail mit **literalen Anführungszeichen** (`"In diesem Bereich…"`) gespeichert.~~ → Root Cause: der 10-Zeichen-Prefix-Match in [hallucinationRate.ts](../src/services/__evals__/interview/scorers/hallucinationRate.ts) `quoteFoundInTranscript()` scheiterte an den umschließenden Quote-Zeichen, obwohl der Inhalt wörtlich im Transkript vorkam. Induziert durch das Analyst-Prompt-Beispiel `z.B. "100", "5 Minuten", "SAP FI"` ([interviewAnalyst.ts:164](../src/services/interviewAnalyst.ts)) — gemini-3.1-flash-lite kopierte den illustrativen Quote-Stil manchmal in den tatsächlichen `evidence_span`-Wert. Fix: zwei Stellen — (1) `quoteFoundInTranscript()` strippt jetzt umschließende Anführungszeichen (straight/curly/deutsch/Guillemets) vor dem Prefix-Match; (2) Prompt-Beispiel auf `z.B. 100 · 5 Minuten · SAP FI` ohne Quotes umformuliert plus explizites "OHNE umschließende Anführungszeichen", damit das Modell den Stil nicht weiter nachahmt. 5 neue Unit-Tests in `hallucinationRate.test.ts` (Quote-Wrap bei Potenzial- und Tazite-Slots, echte Fabrikation bleibt weiterhin erkannt, Backfill-Marker/kurze Quotes bleiben sicher). **Live-verifiziert 2026-06-27**: `hallucination_rate` in allen 3 Läufen 0–0.04 (vorher 0.17 im betroffenen Lauf), keine Quote-Artefakte mehr in den Slot-Trails. | Eval 2026-06-26 (Befund), 2026-06-27 (Fix + Live-Verifikation) | — (gelöst 2026-06-27) |
| KI-17 | ✅ Resolved | ~~Eval-Batches 2026-06-27 (vertriebler, it-support; je 3 Läufe, seed 42): je 1/3 FAIL exakt am `blocked_rate`-Gate (`<0.10`) — vertriebler run1 0.10, it-support run1 0.11, alle anderen Gates grün.~~ → Root Cause: `quick`-Extract ([interviewQuickExtract.ts](../src/services/interviewQuickExtract.ts)) ignoriert sein eigenes SLOT-GUARD-Prompt-Gebot ("Nur NULL-Slots befüllen") wenn die Persona eine bereits erfasste Zahl in anderer Formulierung wiederholt (z.B. "15-20 Tickets täglich" erneut nach bereits gesetztem `frequency_per_month`) — jeder Versuch erreicht `slotConflictResolver.canOverwrite()`, wird dort korrekt geblockt (Prioritäts-Hierarchie greift), erzeugt aber pro Versuch ein `blocked:true`-Trail-Event. Da JEDES Blocked-Event laut Code-Pfad ([applyIntent.ts](../src/services/turnStore/applyIntent.ts)) zwingend ein Prioritäts-Overwrite-Versuch auf einen bereits gefüllten potenzial-Slot ist, misst `blockedRate` exakt diese Prompt-Compliance-Lücke des lite-Modells — kein Fehlalarm der Metrik. Fix: programmatischer Guard in `runQuickExtract()` — `isAlreadyFilledPotenzialSlot()` prüft vor jedem `record_slot`-Aufruf gegen den übergebenen `stepTracker`-Snapshot, ob Step+Slot (potenzial-Slots, nicht tazite — die sind nicht prioritäts-gegated) bereits einen Wert trägt und `is_correction` nicht gesetzt ist; falls ja, kurzschließt der Wrapper mit einer Skip-Antwort BEVOR der Call den Conflict Resolver erreicht. Echte Korrekturen (`is_correction=true`) bleiben unverändert möglich. 9 neue Unit-Tests in `interviewQuickExtract.test.ts` (reine Guard-Funktion + Integrationstest über echten `buildTools`+`MemoryTurnStore`-Pfad: geblockter Fall bleibt unverändert im Snapshot, Null-Slot-Fall und `is_correction`-Fall laufen durch). **Live-verifiziert 2026-06-28** (`--personas it-support/vertriebler --runs 3 --seed 42`, je 2 Batches): `blocked_rate` durchgehend 0–0.02 in beiden Personas (vorher 0.10–0.11 am Gate), **3/3 PASS in beiden Personas** (vorher je 2/3). Separater Fix-Versuch am selben Tag für das davon unabhängige `talker_grounding_violations`-Problem (Talker schreibt der Persona abgeleitete/erfundene Zahlen als Zitat zu, z.B. "Du hast vorhin 350 Tickets pro Monat erwähnt") verursachte eine messbare `dialog_naturalness`-Regression (0.67→0.33 Median bei it-support) und wurde noch am selben Tag wieder zurückgerollt, nachdem der Re-Run mit reaktiviertem Prompt `dialog_naturalness` wieder bei 0.67 bestätigte — dieser Teil bleibt offen, siehe KI-18. | Eval 2026-06-27 (Befund), 2026-06-28 (Fix + Live-Verifikation) | — (gelöst 2026-06-28) |
| KI-19 | ✅ Resolved | ~~Interview endet auf unbeantworteter offener Frage statt Verabschiedung — DB-Status wird `completed` gesetzt, obwohl der letzte sichtbare `agentText` eine Frage ist (meist die PFLICHT-Wrap-up-Sonde aus [talkerPrompt.ts:258](../src/services/talkerPrompt.ts#L258)). Cross-Modell-Check 2026-07-11: 36/82 (44%) aller `google/gemini-3.1-flash-lite`-Transkripte (Demo-Baseline) betroffen — NICHT OSS-/OpenRouter-spezifisch.~~ → Root Cause 2026-07-11: der skriptete Abschluss-/Farewell-Call in [runInterviewTurn.ts](../src/services/runInterviewTurn.ts) (nach `checkLifecycle` → `shouldComplete=true`, DB-Status bereits `completed`) übergab `phase: 'wrap_up'` genau wie ein normaler Turn an `createTalkerStream` — und erbte dadurch dieselbe UNBEDINGTE PFLICHT-Methodik-Instruktion "Stelle als allererste Antwort in dieser Phase exakt diese Frage" aus [talkerPrompt.ts buildPhaseMethodology()](../src/services/talkerPrompt.ts#L190), die die weichere `farewellBriefing.suggested_question` ("Verabschiede dich kurz und herzlich") regelmäßig überstimmte — das Modell fragte erneut statt sich zu verabschieden. Fix: neues Feld `isCompletionFarewell?: boolean` auf `InterviewContext` ([interviewTypes.ts](../src/services/interviewTypes.ts)), gesetzt ausschließlich am Abschluss-Call-Standort in `runInterviewTurn.ts`; `buildPhaseMethodology()` liefert bei gesetztem Flag einen eigenen wrap_up-Abschluss-Text ohne PFLICHT-Frage-Zwang, stattdessen "Verabschiede dich jetzt kurz und herzlich... KEINE weitere Frage". 6 neue/erweiterte Unit-Tests (`talkerPrompt.test.ts` neu, `runInterviewTurn.test.ts` T4 erweitert), 869/870 Gesamttests grün (1 Skip vorbestehend), `tsc --noEmit` grün. **Live-verifiziert 2026-07-11** (`--personas buchhalter --runs 3 --seed 42 --store pglite`, `google/gemini-3.1-flash-lite`): **3/3 Läufe enden auf einer echten Verabschiedung**, 0/3 auf einer unbeantworteten/dangling Frage (vorher 36/82 historisch = 44%). Beispiel Run3 letzter Turn: "Vielen Dank für deine Offenheit und die wertvollen Einblicke... Damit sind wir am Ende unseres Gesprächs angelangt. Ich wünsche dir noch einen produktiven Tag." Alle 3 Läufe PASS, `completion_correctness: 1`, `dialogNaturalness` Median 0.67 (Gate ≥0.65, keine Regression), `hallucinationRate`/`talkerGroundingViolations` beide 0. | Qualitative Transkript-Analyse 2026-07-06/07 (Befund), Cross-Modell-Check 2026-07-11 (82 historische Transkripte), Fix + Live-Verifikation 2026-07-11 | — (gelöst 2026-07-11) |
| KI-20 | ✅ Resolved | ~~Rohe Tool-Call-Syntax erscheint als sichtbarer Freitext statt strukturiertem Tool-Call (z.B. `[update_walkthrough_data(step_title="...", ...)]` vor der eigentlichen Frage, bis zu 49× in einem minimax-m3-Lauf beobachtet). Cross-Modell-Check 2026-07-11: 11 von 14 betroffenen historischen Transkripten sind `google/gemini-3.1-flash-lite`/`google/gemini-3.5-flash` (Demo-Baseline!) — NICHT modellspezifisch.~~ → Root Cause 2026-07-11: das Few-Shot-Beispiel `WALKTHROUGH_EXAMPLES` in [talkerPrompt.ts:266-283](../src/services/talkerPrompt.ts#L266) zeigte selbst die illustrative Notation `AGENT: [ruft sofort update_walkthrough_data(...) auf]` inline im Beispieltext — Modelle über mehrere Vendor-Grenzen hinweg ahmten diese Klammer-Pseudo-Code-Notation gelegentlich wörtlich als sichtbaren Output nach (gleiche Fehlerklasse wie KI-16: Anführungszeichen aus einem illustrativen Prompt-Beispiel wörtlich in `evidence_span` übernommen). Fix: Beispieltext auf reine Prosa-Beschreibung ohne Klammer-/Parameter-Syntax umgeschrieben, explizit als "INTERNER TOOL-CALL (lautlos, NIEMALS als Text ausgeben)" markiert; zusätzlich neue Defense-in-Depth-Regel im `STATIC_PROMPT` ([talkerPrompt.ts:42](../src/services/talkerPrompt.ts#L42)), die Tool-Namen/Klammer-/Parameter-Syntax in der sichtbaren Antwort generell verbietet (nicht nur für `update_walkthrough_data`). 4 neue Unit-Tests (`talkerPrompt.test.ts`), 869/870 Gesamttests grün, `tsc --noEmit` grün. **Live-verifiziert 2026-07-11** (`--personas buchhalter --runs 3 --seed 42 --store pglite`, `google/gemini-3.1-flash-lite`, derselbe Batch wie KI-19): **0 Treffer** für `[record_slot`, `[update_walkthrough_data`, `[register_step`, `[ruft `, `[/TOOL_CALL]`, `[record_dependency` über alle 3 Transkripte — keine sichtbare Tool-Call-Syntax mehr. War der ursprüngliche Auslöser der gesamten Bug-Triage-Roadmap, Priorität-1-Fix abgeschlossen. | Qualitative Transkript-Analyse 2026-07-07 (Befund), Cross-Modell-Check 2026-07-11 (139 historische Transkripte), Fix + Live-Verifikation 2026-07-11 | — (gelöst 2026-07-11) |
| KI-23 | ✅ Resolved | ~~Wrap-up erreicht nie `completed` — Interview bleibt in Phase `wrap_up` hängen. Real verifiziert am Supabase-Interview `09c2052c…` ("Tim", 2026-07-14), `status` blieb `active` trotz vier Verabschiedungen. Doppelursache: Completion an `semanticAllStepsDone` gekoppelt (praktisch nie true) + brittle `FAREWELL_MARKERS`-String-Match; content-blinder `computeTurnBudget`-Turn-Count-Trigger löste Wrap-up schon nach ~9 Turns aus.~~ → PROJ-42 (2026-07-16) entfernte `computeTurnBudget`/`FAREWELL_MARKERS` ersatzlos, führte den Phasen-Kollaps + eine deterministische State-Terminierung ein (Tim-Regressionstest gegen die echten Supabase-Daten). PROJ-46 (Strom D, Closing-Probe-Entfernung) löste danach eine Zwischenregression aus (6/6 Läufe `completion_correctness=false` — das Analyst-„fertig"-Urteil blieb in Phase `explore` wirkungslos), behoben durch den B/C-Fix: `discovery_exhausted` als geguardetes Analyst-Signal an `resolveTurnLifecycle` (ADR-024). **Live-verifiziert PROJ-46 QA-Runde 3 (2026-07-19):** completion 8/8 via Readiness, 0× Timer-Force, turnsToCompletion 14–21 (vorher 6/6 Non-Termination). Code-Check 2026-07-22 bestätigt: `computeTurnBudget`/`FAREWELL_MARKERS` existieren im Code nicht mehr. | Realer UI-Durchlauf 2026-07-14 | — (gelöst 2026-07-19, PROJ-42+PROJ-46) |
| KI-24 | ✅ Resolved | ~~Rollenverletzung — der Interviewer beantwortet fachfremde Rückfragen der interviewten Person. Tim Turns 15-17: VW-Golf-Preis (28.000 €), Flugpreise (100-300 €), beide beantwortet. Kein Guard gegen Rückfragen, Rolle nur eine funktionale Zeile.~~ → PROJ-42 (Strom 2, 2026-07-16): neues `roleGuard.ts` (Prefilter→Judge→Redirect), hookt vor Quick-Extract/Talker/Analyst als frühester Gate im Turn-Pfad. Kernproblem (kein Guard vorhanden) strukturell behoben. Code-Check 2026-07-22 bestätigt `roleGuard.ts` weiterhin aktiv. Verbliebene Präzisions-Schwäche des Guards (Falschpositive bei genuinen, aber ausweichenden Mitarbeiter-Antworten) läuft eigenständig weiter als **KI-26** in `features/INDEX.md`. | Realer UI-Durchlauf 2026-07-14 | — (gelöst 2026-07-16, PROJ-42) |
| KI-27 | ✅ Resolved | ~~Sprunghafte Themenführung ohne Vertiefung — der Interviewer springt zwischen Prozessen, bevor einer ausexploriert ist (PROJ-44-QA-Eval, Ping-Pong bei buchhalter/it-support).~~ → PROJ-44 führte den O-Drought-Fokus-Lock ein (hob die O-Feld-Tiefe messbar, löste das Ping-Pong aber noch nicht strukturell — der Lock war nur advisory im Analyst-Prompt, der Talker behandelte die Empfehlung als unverbindlich). PROJ-46 (Rollen-Vertrag-Amendment, ADR-023) machte den Lock über `buildZielBlock` **bindend für den Talker**. **Live-verifiziert PROJ-46 QA-Runde 3 (2026-07-19):** "M-6 (Ping-Pong) transkript-verifiziert weg", keine Rücksprünge trotz Lock mehr beobachtet. Code-Check 2026-07-22 bestätigt `buildZielBlock` aktiv in `talkerPrompt.ts`. Formaler Status-Übergang der Elternfeatures (PROJ-42/44/46 In Review → Approved) folgt separat beim gemeinsamen Joint-Gate nach PROJ-43 — das war eine bewusste Feature-Bookkeeping-Sequenzierung, kein offener Zweifel am KI-27-Befund selbst. | PROJ-44-QA-Eval 2026-07-17 | — (gelöst 2026-07-19, PROJ-46) |
| KI-22 | ✅ Resolved | ~~Beim erneuten Öffnen/Reload eines laufenden Interviews (`/api/interview/[token]/reconnect`) wurde die zuletzt gestellte, noch unbeantwortete Interviewer-Frage als NEUE, fast wortgleiche Chat-Bubble erneut ausgegeben — sichtbares Duplikat direkt untereinander (manueller UI-Test 2026-07-07/11, Interview "Hans Becker": "Du hast vorhin 180 Rechnungen..." erscheint zweimal in Folge).~~ → Root Cause: `turns` werden ausschließlich als atomare `(user_input, agent_response)`-Paare persistiert ([store.insertTurn](../src/services/runInterviewTurn.ts)), ein Turn wird erst nach der Agent-Antwort geschrieben — die von `reconnect/route.ts` gebaute `history` endet dadurch STRUKTURELL IMMER mit `role: 'assistant'` (der Fall "letzte Nachricht vom Mitarbeiter" kann mit dieser Persistenz gar nicht auftreten, ein unbeantworteter Mitarbeiter-Turn wird nie in die DB geschrieben). Der Reconnect-Handler rief bislang trotzdem immer das LLM auf (`createInterviewStream`, der ALTE Prä-Dual-Loop-Pfad aus `interviewAgent.ts` — läuft komplett außerhalb von `runInterviewTurn.ts`/Analyst/Quick-Extract/KI-18-Guard, deckungsgleich mit der bereits dokumentierten PROJ-37-Prompt-Drift) mit einer synthetischen "Ich bin wieder da, können wir weitermachen?"-Nutzernachricht — das Modell posierte daraufhin naheliegend die noch offene Frage erneut, was oben auf die bereits sichtbare History gerendert wurde. Fix: `reconnect/route.ts` prüft jetzt explizit `history[history.length-1].role`; ist sie (wie strukturell immer) `'assistant'`, wird direkt eine kurze statische Re-Engagement-Zeile zurückgegeben, OHNE LLM-Call (kein Fabrikationsrisiko, kein Kosten-/Latenz-Overhead bei jedem Seiten-Reload) — der bisherige LLM-Pfad bleibt als defensiver Fallback für den (aktuell unmöglichen) Fall `role==='user'` erhalten. 1 Test aktualisiert + 1 neuer Test in `reconnect.test.ts` (bestätigt: kein `createInterviewStream`-Aufruf, Antworttext ≠ wiederholte alte Frage), 870/871 Gesamttests grün (1 Skip vorbestehend), `tsc --noEmit` grün. **Architektur-Nebenbefund (nicht in dieser Session behoben, für Meilenstein 5 vorgemerkt):** der komplette Reconnect-Pfad umgeht die Dual-Loop-Pipeline strukturell — jeder KI-18/19/20-Fix dieser Session griff dort NICHT, weil `interviewAgent.ts` ein eigenständiger, seit PROJ-37 bekannt auseinandergelaufener Prompt-/Tool-Pfad ist. Für den Demo-Fall selbst unkritisch (der jetzige Fix umgeht das LLM im Regelfall komplett), aber eine offene Architektur-Schuld. | Manueller UI-Test 2026-07-07/11 (Befund), Fix + Unit-Tests 2026-07-11 | — (gelöst 2026-07-11) |

## Joint-Gate 2026-07-24 — geschlossene KIs (KI-21, KI-25, KI-29, KI-30)

> Im Joint-Gate PROJ-40/42/43/44/46/48 (2026-07-24) verlustfrei aus `features/INDEX.md` hierher verschoben. **KI-29** (Fokus-Lock-Bootstrap) + **KI-30** (verfrühter Abschluss) via PROJ-48 behoben und Eval-belegt (6 frische Läufe: 3 buchhalter run1 27→22/run3 29→21 Turns, 3 it-support run2 PASS; step_registration 1.0, kein Reopening, dialog_naturalness bis 1.0). **KI-21** + **KI-25** (Forced-Choice-Anchoring / -Wiederholung) via PROJ-43 strukturell gelöst — Forced-Choice am 2026-07-24 code-verifiziert vollständig aus `talkerPrompt.ts` entfernt (nur noch Verbots-/Richtungsfrage-Text ohne Zahlen), `anchoring_violations` 0 in buchhalter + it-support. Bekannte Restpunkte: KI-29-Ordering/Rotations-Fairness → PROJ-49; PROJ-43 AC7 (ungetriggert) als Folge-KI.

| ID | Severity | Beschreibung | Entdeckt in | Fix-Aufwand |
|----|----------|-------------|-------------|-------------|
| KI-29 | ✅ Resolved (2026-07-24) | **O-Drought/Fokus-Lock Bootstrap-Fehler** — wenn der Analyst im allerersten Turn (leerer Tracker) mehrere Schritte gleichzeitig registriert (der Normalfall: die meisten Mitarbeiter nennen 2+ Aufgaben in ihrer Eröffnungsantwort), verliert das System alle bis auf einen davon sofort aus den Augen. Ursache: `computeFocusLock` bootstrapt bei leerem Tracker mit `{stepId: null}` ([interviewOrchestrator.ts:126-147](../src/services/interviewOrchestrator.ts#L126)). Wenn der Analyst danach im selben Turn mehrere Schritte registriert, kürzt `updateODrought` sofort ab — `if (lock.stepId == null) return lock` ([interviewOrchestrator.ts:188](../src/services/interviewOrchestrator.ts#L188)) — und gibt den eingehenden Null-Lock unverändert zurück, OHNE zu prüfen, ob im `after`-Tracker inzwischen echte Schritte mit Inhalt existieren. Das persistierte `oDrought` bleibt `{stepId: null}`. Der nächste Turn berechnet den Lock dadurch komplett neu (`previous.stepId` ist null → kein "stillLocked") und fällt auf `candidates[0]` zurück — schlicht der Schritt, der zufällig zuerst im Array steht (= Registrierungsreihenfolge der `register_step`-Tool-Calls), unabhängig davon, worüber der Mitarbeiter gerade tatsächlich gesprochen hat. Zusätzlich: `buildZielBlock` ([talkerPrompt.ts](../src/services/talkerPrompt.ts), `if (!focusStep) return ''`) liefert für genau diesen ersten Talker-Call GAR KEIN "Ziel (bindend)"-Briefing — der Talker entscheidet komplett frei, worüber er als Nächstes fragt. **Live-Beleg** (buchhalter-Eval 2026-07-23, interview_id `34ca9cd4-17ea-41e1-ba8b-5c871ad92e70`, verifiziert via Langfuse-Trace, zeitlich sortiert): Turn-1-Antwort der Persona deckt sowohl Rechnungsprüfung als auch Monatsabschluss ab; Talker stellt genau EINE Frage zu Monatsabschluss (Teilschritte), danach nie wieder — der Turn-1-Talker-Call hatte nachweislich keinerlei Ziel-Block. Ab Turn 2 lockt das System auf Rechnungsprüfung (`candidates[0]`, reine Registrierungsreihenfolge-Artefakt) und bleibt dort für den Rest des Interviews. Finale O-Feld-Abdeckung (von 10 O2-O6-Feldern): Rechnungsprüfung 6/10, Monatsabschluss **3/10** — trotz vollständiger, substantieller Ersteinlassung der Persona zu Monatsabschluss. Root aus PROJ-44 (Commit `674fb38`, "H-1/M-1/M-3 Remediation — O-Drought-Fokus-Lock"), von PROJ-43 nicht berührt (verifiziert: PROJ-43-Diff ändert weder `computeFocusLock` noch `updateODrought`). Fix-Richtung (nicht präskriptiv): nach dem Online-Pass braucht es einen Pfad, der bei `lock.stepId == null` UND neu registrierten/befüllten Schritten im `after`-Tracker aktiv einen frischen Lock etabliert, statt den Null-Lock unverändert durchzureichen — z.B. `computeFocusLock` nach dem Analyst-Commit ein zweites Mal gegen den aktualisierten Tracker aufrufen, statt sich allein auf `updateODrought`s Streak-Fortschreibung zu verlassen. **Update 2026-07-23:** zusätzlich real bestätigt in einem echten Praxis-Interview ("Giorgia", Supabase `bcecd7ba-56f7-4465-910b-6b51184dce40`) — dominanter Recruiting-Prozess verliert gegen einen in einem Nebensatz erwähnten Event-Organisations-Prozess, exakt nach demselben Mechanismus. Architektur + Fix-Design in `/grilling`-Runde festgelegt (Recompute-nach-Commit + Salienz-Tie-Break über `countFilledOFields`), siehe [PROJ-48](interview-engine/PROJ-48-fokus-lock-haertung.md). **Update 2026-07-23 (spätere Session):** Bootstrap-Teil behoben — `updateODrought` ruft bei `lock.stepId == null` jetzt `computeFocusLock(afterTracker, lock)` auf (Recompute-nach-Commit, Talker-Ziel-Block ab Turn 1). Der `Salienz-Tie-Break` (`pickMostSalientCandidate`) wurde jedoch wieder **entfernt** — er bevorzugte den schon-am-weitesten-abgedeckten Schritt und ließ frisch genannte 0-Feld-Schritte warten (falsche Richtung), `computeFocusLock` fällt wieder auf `candidates[0]` zurück. Das eigentliche „Agent eröffnet auf dem falschen (weniger KI-relevanten) Prozess" ist damit NICHT gelöst (Bootstrap ist Gleichstand → Registrierungsreihenfolge) → verschoben nach **PROJ-49** (Prozess-Priorisierung). buchhalter-Eval-belegt: Talker bekommt ab Turn 1 einen Ziel-Block. | PROJ-43-QA-Eval 2026-07-23 (buchhalter, Langfuse-Trace-Analyse nach Nutzer-Rückfrage zur Turn-Anzahl); Giorgia-Praxisinterview 2026-07-23 (Supabase-Verifikation) | → PROJ-48 (Bootstrap) / PROJ-49 (Ordering) |
| KI-30 | ✅ Resolved (2026-07-24) | **`discovery_exhausted` überspringt den `hasUnexhaustedStep`-Check** — der Analyst kann das Interview in Richtung Abschluss schicken, obwohl gerade erst neu entdeckte oder kaum explorierte Schritte offen sind. In `resolvePhaseTransition`s `explore`-Case prüft der `discovery_exhausted`-Zweig ([interviewOrchestrator.ts:267](../src/services/interviewOrchestrator.ts#L267)) nur `hasSubstantialCoverage(ctx.stepTracker)` — d.h. `stepTracker.some(...)`, **ein einziger** Schritt mit ≥2 gefüllten O-Feldern reicht. Der parallele `step_advance_ready`-Zweig direkt darunter ([Zeile 279](../src/services/interviewOrchestrator.ts#L279)) prüft zusätzlich `!hasUnexhaustedStep(...)` — dieser Check fehlt beim `discovery_exhausted`-Zweig komplett. **Live-Beleg** (gleicher buchhalter-Eval wie KI-29): Turn 7 — der Analyst registriert "Mahnlauf" (allererste Erwähnung) UND setzt `discovery_exhausted: true` in **derselben** `produce_briefing`-Tool-Call-Batch, bevor auch nur eine einzige Frage zu Mahnlauf gestellt wurde. Finale Mahnlauf-Abdeckung: **0/10** O-Felder. Root aus PROJ-46/ADR-024 (Commit `4ff0b83`, "Completion-Readiness via discovery_exhausted"), von PROJ-43 nicht berührt. Verhältnis zu KI-29: unabhängig fixbar — `hasUnexhaustedStep` liest `oDrought.exhaustedStepIds`/`streak`, nicht die (von KI-29 betroffene) Lock-Zuweisung selbst; ein Schritt, der nie gelockt wurde (wie Monatsabschluss in Turn 2-6), zählt trotzdem korrekt als "nicht exhausted" (`status!=='done' && !exhausted.has(id) && !isFullyCovered(step)` → true). Ein reiner KI-30-Fix hätte also in diesem konkreten Transkript den verfrühten Turn-7-Abschluss bereits verhindert, auch ohne KI-29 anzufassen — beide sollten trotzdem in derselben Session untersucht werden, da sie dieselbe Subsystem-Ebene (O-Drought/Fokus-Lock, PROJ-44/46) betreffen. Fix-Richtung: den `discovery_exhausted`-Zweig um dieselbe `!hasUnexhaustedStep(ctx.stepTracker, ctx.oDrought)`-Bedingung wie Zeile 279 ergänzen. **Update 2026-07-23:** zweiter unabhängiger Live-Beleg im selben buchhalter-Transkript gefunden — "Mahnlauf" (Turn 7) bekommt im selben Turn ein Potenzial-Feld (`frequency`) gefüllt, wodurch der Status sofort `exploring`→`walkthrough` wechselt (verifiziert in `applyIntent.ts`: ein einziges Feld reicht, auch Potenzial-Felder zählen). Die Closing-Reentry-Sicherung (Zeile 293, `hasStepInStatus('exploring') || hadExtractionThisTurn`) greift dadurch nicht — Terminal-Check schließt das Interview im selben Turn ab, Mahnlauf bleibt bei 0/10 O-Feldern. Fix-Scope erweitert um Zeile 293 (`|| hasUnexhaustedStep(...)`); kein separater Terminal-Check-Fix nötig (Zeile-293-Fix sichert ihn transitiv ab, siehe [PROJ-48](interview-engine/PROJ-48-fokus-lock-haertung.md)). **Update 2026-07-23 (spätere Session): der `hasUnexhaustedStep`-Fix war überschießend und verursachte eine Abschluss-Regression** — `hasUnexhaustedStep` (= irgendein Schritt nicht voll 10/10) ist bei 4–6 einzeln abgearbeiteten Prozessen fast immer `true`, wodurch das Interview nie sauber schloss (buchhalter run2 verabschiedete sich und öffnete in Turn 29 wieder; run3 zog sich durch 4 Goodbye-Turns). **Verengt zu `hasUnexploredStep`** (= irgendein Schritt mit `countFilledOFields === 0`, exhausted ausgenommen) an denselben zwei Stellen — fängt weiterhin den 0/10-Steamroll (Mahnlauf 0 O-Felder blockiert Abschluss), blockiert aber nicht mehr wegen teil-abgedeckter Schritte. buchhalter-Eval-belegt: run1 27→22, run3 29→21 Turns, kein Reopening, dialog_naturalness 0.67→1.0. Damit **behoben** (PROJ-48 In Review wegen orthogonalem `dedup_slot_coverage`-Gate). | PROJ-43-QA-Eval 2026-07-23 (buchhalter, Langfuse-Trace-Analyse) | → PROJ-48 |
| KI-25 | ✅ Resolved (2026-07-24) | Unpassende, anchoring-anfällige Nachfragen — Forced-Choice greift blind auch auf Unsinn/Nicht-Antworten (Tim Turn 3 "der toilettengang" → Forced-Choice zur Code-Frequenz; Turn 8 "5" → "Eher 2 Stunden oder eher 6 Stunden?" trotz gerade genannter Zahl); Frage-Wiederholung durch offenen State (Turn 17 = Wrap-up-Frage aus Turn 9). **Update 2026-07-22:** der ursprünglich mitgenannte "Quick-Extract-1-Turn-Race"-Anteil ist gegenstandslos — `interviewQuickExtract.ts` wurde in PROJ-44 (2026-07-17) komplett gelöscht, der Mechanismus existiert nicht mehr. Verbleibender Scope (Forced-Choice-Anchoring + Frage-Wiederholung) ist jetzt Gegenstand von PROJ-43 (`/architecture` 2026-07-23 abgeschlossen, Status Architected): offene Frage → Richtungsfrage → Clarification Card, kein Forced-Choice mehr an keiner Stelle. → **PROJ-43**. | Realer UI-Durchlauf 2026-07-14 | → PROJ-43 |
| KI-21 | ✅ Resolved (2026-07-24) | Forced-Choice-Pattern ("Eher X oder eher Y?", [talkerPrompt.ts:45](../src/services/talkerPrompt.ts#L45), bewusst eingeführt in [PROJ-29](interview-engine/PROJ-29-gesprächsführungs-revision.md) nach einem Eval-Fund 2026-06-23, bei dem offene Umformulierung zu dreifacher Wiederholung ohne Werterhalt führte) ist methodisch ein Anchoring-Risiko: die Frageform selbst liefert der Persona zwei konkrete Zahlen, bevor sie geantwortet hat. `anchoringViolationRate` ([detectNumberAnchoring in interviewTalker.ts](../src/services/interviewTalker.ts#L55) + [scorers/index.ts](../src/services/__evals__/interview/scorers/index.ts#L78)) prüft nur, ob ein vom Analyst vorgeschlagener Wert in die Frage durchsickert — nicht das strukturell im Prompt selbst angelegte Forced-Choice-Muster. **Update 2026-07-22:** ursprünglich als bewusste Design-Abwägung ohne Fix eingestuft ("Ausweich-Vermeidung wichtiger als das Restrisiko") — durch PROJ-43 (Elicitation-Reorientierung, Status Architected seit 2026-07-23) überholt: Forced-Choice wird dort vollständig entfernt (offene Frage → Richtungsfrage ohne vorgeschlagene Zahlen → Clarification Card, kein Zahlen-Vorschlag durch den Talker an keiner Stelle mehr). Bleibt offen bis PROJ-43 gebaut und verifiziert ist. | Qualitative Transkript-Analyse 2026-07-06/07, eingeordnet in Bug-Triage 2026-07-11 | → PROJ-43 |

---

## KI-18 — Grounding-Violations Fix-Historie (archiviert 2026-07-24, KI-18 bleibt OFFEN)

> Aus `features/INDEX.md` ausgelagert am 2026-07-24 (Joint-Gate-Cleanup). KI-18 selbst bleibt **offen** (kondensierter Eintrag + aktuelle Bewertung in INDEX.md). Hier die vollständige Fix-Versuch-Historie (Versuche 1–6, Modell-Klärung, Bestandsaufnahme, Doku-Funde) verlustfrei.

**Fünfter Fix-Versuch 2026-06-30 abends** (drei Iterationen am selben Abend, alle live-verifiziert, `--models google/gemini-3.1-flash-lite` durchgehend): Ausgangspunkt war der unter "Vierter Fix-Versuch" dokumentierte Restbefund — Run1 der ersten Nachmittags-Verifikation zeigte einen NEUEN Fehlertyp (Wortwahl-Zuschreibung: Agent behauptet, Mitarbeiter habe Wort "unmöglich" schon einen Turn früher benutzt, tatsächlich war Turn 9 nur eine Umschreibung). **Iteration A:** `GUARD_SYSTEM` in [talkerGroundingGuard.ts](../src/services/talkerGroundingGuard.ts) um eine 4. Verletzungsklasse "WORTWAHL-ZUSCHREIBUNG" mit diesem Fall als Few-Shot erweitert. Re-Verifikation (it-support, `--runs 5 --seed 99`) deckte einen unabhängigen, vorbestehenden Bug auf: der Parser-Fallback (`parseGuardResponse`) loggte jetzt sichtbar `unexpected judge format, fallback no-violation` — der Judge-Call wurde vermutlich durch `maxOutputTokens: 300` mitten im `reason`-Feld abgeschnitten (der jetzt 4-Klassen-Prompt ermutigt zu längeren Begründungen), wodurch ein echter Violation-Fall ungeprüft durchgeliefert wurde (Agent deutete die Höflichkeitsfloskel "Kein Problem" der Persona fälschlich als Sachaussage um). **Iteration B:** `parseGuardResponse` bekommt eine Partial-Parse-Recovery — wenn das volle JSON nicht schließt, wird das `"violation"`-Feld per Regex isoliert extrahiert (es steht im Schema immer zuerst und übersteht eine Kürzung meist), `maxOutputTokens` auf 500 erhöht, Prompt-Anweisung für `reason` auf "max. 1 kurzer Satz" verschärft. Re-Verifikation (gleicher Seed 99, 5 Läufe) zeigte: Guard erkannte jetzt deutlich mehr reale Fälle (alle vom Typ Zahl-Fabrikation, vorher vom Truncation-Bug verschluckt) — aber `dialog_naturalness`-Median fiel bei identischem Seed von 0.67 (vor Iteration B) auf 0.33 (3 von 5 Läufen statt 1 von 5 unter dem ≥0.65-Gate). Judge-Begründungen der betroffenen Läufe: "abrupte Themensprünge", "Fragenkatalog statt Gespräch", "mehrfache identische Abschlusssätze" — Ursache: die alte Repair-Anweisung in [interviewTalker.ts](../src/services/interviewTalker.ts) bot als Ausweg "stelle eine neue Frage ohne Rückbezug" an, was der Talker unter dem jetzt häufigeren Korrekturdruck zunehmend nutzte und dadurch generische, themenfremde Anschlussfragen produzierte (kein Treffer war auf Wortwahl-Zuschreibung — alle weiterhin Zahl-Fabrikation, die Klasse 4 war nicht die Ursache der zusätzlichen Repairs). **Iteration C:** Repair-Anweisung umformuliert — verbietet explizit eine Wiederholung des fehlerhaften Werts, fordert thematische Kontinuität statt Themenwechsel, behält denselben natürlichen Ton; KEINE Änderung an `talkerPrompt.ts`/`STATIC_PROMPT`. Re-Verifikation (gleicher Seed 99, 5 Läufe): `dialog_naturalness`-Median erholt auf 0.67 (Min 0.33, Max 1.0 — kein Hinweis auf neue Regression), `talker_grounding_violations`-Median auf 0 gefallen (3 von 5 Läufen 0 Violations, vorheriger Baseline-Median war 1). 27 Unit-Tests insgesamt für `talkerGroundingGuard.ts`/`interviewTalker.ts` (inkl. Truncation-Recovery- und Repair-Prompt-Content-Tests), alle grün, `tsc --noEmit` grün. **Verbleibender Restbefund** (2 von 5 Läufen in der finalen Verifikation, je 1–2 Violations): ein fünfter, noch nicht im Guard-Prompt abgedeckter Fehlertyp — der Agent konstruiert einen falschen Widerspruch zwischen zwei Mitarbeiter-Aussagen, entweder indem er eine Aussage aus dem AKTUELLEN Turn fälschlich als "vorhin" einordnet und sie einer früheren Aussage gegenüberstellt (obwohl kein Widerspruch besteht, da beide Aussagen separat korrekt sind), oder indem er zwei unterschiedliche Kategorien (z.B. "Tickets/Tag" vs. "Hardware-Tausch/Woche") als dieselbe Metrik verwechselt. Verwandt mit Klasse 4 (Timing-Fehler bei "vorhin"), aber nicht deckungsgleich. Nutzer-Entscheidung 2026-06-30: nicht weiter iterieren (abnehmender Ertrag, Eval-Kosten) — Status bleibt **Open**, Restmuster hier dokumentiert für eine künftige 6. Iteration. Insgesamt 18 Live-Eval-Läufe an diesem Abend (it-support, flash-lite, seeds 42+99) zur Verifikation der drei Iterationen. Artefakte: `docs/evals/interview/2026-06-30/2026-06-30-{17,18,19,20}-*-google-gemini-3-1-flash-lite-it-support-*.md`

**Modell-Spezifitäts-Klärung 2026-07-11** (Bug-Triage vor Pilot-Demo, Nutzer-Rückfrage ob MiniMax-spezifisch): Antwort ist eindeutig modellübergreifend — und zwar bereits vor der heutigen Klärung bekannt. Die gesamte bisherige KI-18-Historie oben (alle fünf Fix-Versuche + alle Live-Verifikationen) wurde ausschließlich auf `google/gemini-3.1-flash-lite` (der Demo-Baseline) gefunden und bearbeitet, nie auf einem OSS-Modell. Neue Evidenz aus dem PROJ-41-Pass-B-lite-Vergleich (2026-07-07, [PROJ-41-passB-lite-buchhalter-pinned.md](../docs/evals/PROJ-41-passB-lite-buchhalter-pinned.md)): derselbe Fehlermechanismus trat 14× im Live-Guard bei `openrouter/deepseek/deepseek-v4-pro` auf (12× erfolgreich repariert, 2× nach Erschöpfen der Repair-Versuche trotzdem ausgeliefert), bei `openrouter/minimax/minimax-m3` dagegen 0× (`talker_grounding_violations: 0`, keine Guard-Repair-Ereignisse in allen 3 Läufen). Einordnung: KI-18 ist damit modellübergreifend bestätigt (mindestens Google Gemini + DeepSeek), und die Demo-Relevanz bestand bereits vor der heutigen Cross-Modell-Prüfung, da die Baseline selbst seit 2026-06-27 durchgehend betroffen ist — nicht erst durch die heutige OSS-Recherche neu entdeckt.

**Sechster Fix-Versuch 2026-07-11** (Guard-Reliability statt Prompt/Klassifikation — neue Fehlerklasse, im manuellen UI-Test gefunden, nicht in Evals): reales Interview ("Hans Becker", `gemini-3.1-flash-lite`, Turn 2) zeigte live exakt das ursprüngliche KI-9/KI-18-Muster — "Du hast vorhin 180 Rechnungen pro Monat erwähnt — jetzt sprichst du von 8 bis 10 pro Tag" — obwohl der Mitarbeiter nur "8-10 pro Tag" genannt hatte (180 = Talker-eigene, unautorisierte Hochrechnung, dann fälschlich als frühere Mitarbeiter-Aussage zugeschrieben; Root-Cause-Ausschluss: sämtliche Tracker-Snapshot-Pfade in `talkerPrompt.ts` maskieren Rohwerte bereits seit ADR-015/Fix4 — die Zahl "180" ist im Prompt-Kontext an keiner Stelle sichtbar, der Talker hat sie selbst live berechnet). Direkte Reproduktion via `checkGroundingViolation()` mit dem exakten historischen Transkript **erkannte die Verletzung korrekt und zuverlässig** ("Fall 1: ... Agent hat einen Wert fabriziert oder selbst berechnet") — der Guard-Mechanismus/Prompt selbst ist also weiterhin sound, die Klassifikationslogik ist NICHT die Fehlerquelle dieses Vorfalls. Root Cause stattdessen: `checkGroundingViolation()` hatte für einen fehlschlagenden Judge-Call (Netzwerk-Blip, Rate-Limit, transiente Provider-Störung) KEINEN Retry — ein einziger Fehlschlag degradierte lautlos zu `{violation:false}` (nur `console.warn`, nicht von einem echten "keine Verletzung"-Ergebnis unterscheidbar), wodurch die Antwort komplett ungeprüft auslieferte. Gleiche Fehlerklasse wie KI-11 (`withRetry()` in `supabaseTurnStore.ts`), hier aber nie auf den Guard-Judge-Call angewendet. Fix: bounded Retry (1 Wiederholung, 300ms Backoff) direkt in `checkGroundingViolation()` ([talkerGroundingGuard.ts](../src/services/talkerGroundingGuard.ts)); finales Scheitern nach allen Versuchen jetzt `console.error` (nicht `warn`) mit explizitem "shipping WITHOUT grounding check"-Hinweis, damit ein künftiger Ausfall dieser Art nicht mehr lautlos bleibt. 2 neue Unit-Tests (Retry-nach-Erfolg reproduziert exakt den 180-Rechnungen-Fall; beide Versuche schlagen fehl → `console.error` + `violation:false`), 870/871 Gesamttests grün, `tsc --noEmit` grün. Kein Eingriff an `GUARD_SYSTEM`/Klassifikationsprompt — das bestehende Klassifikations-Problem (Kernproblem von KI-18, "Guard-Sensitivität" laut Fazit des 4. Fix-Versuchs) bleibt unverändert **offen**; dieser Fix schließt ausschließlich die neu gefundene Silent-Fail-Open-Lücke bei Judge-Call-Fehlern.

**Historische Bestandsaufnahme 2026-07-12** (Nutzer-Rückfrage: ist die Framing-Mismatch-Hypothese aus dem "180 Rechnungen"-Fund repräsentativ, und wie verbreitet ist KI-18 überhaupt über die Historie?): systematische Prüfung aller 142 vorhandenen Eval-Transkripte — Callback-Muster ("Du hast vorhin...", "du sagtest...", etc.) per Regex vorgefiltert (203 Treffer über 96 Transkripte), jeder Treffer mit dem ECHTEN Produktions-Guard (`checkGroundingViolation()`, gleicher Mechanismus wie in Prod, nicht nur eine Heuristik) gegen die reale Turn-Historie geprüft. **Ergebnis: 39 von 203 (19%) sind echte Verletzungen, verteilt über 30 von 96 Transkripten (31%).** Nach Modell: `google/gemini-3.1-flash-lite` (Demo-Baseline) 29/104 (28%) — trägt **74% aller gefundenen Verletzungen**; `google/gemini-3.5-flash` 8/53 (15%); `anthropic/claude-haiku-4-5` 1/8; `openrouter/deepseek/deepseek-v4-pro` 1/17; `openrouter/minimax/minimax-m3`, `openrouter/deepseek/deepseek-v4-flash`, `openrouter/z-ai/glm-5.2` je 0. Bestätigt nochmal unabhängig von der KI-19/20-Cross-Modell-Prüfung: KI-18 ist überwiegend ein Baseline-Problem, kein OSS-Randbefund. **Klassifikation der 39 Verletzungen nach Mechanismus** (jede einzeln gegen Originalturns gelesen): 13/39 (33%) sind das Framing-Mismatch-Muster aus dem "180 Rechnungen"-Fund (Agent fragt in Zeiteinheit A, Mitarbeiter antwortet in Zeiteinheit B, Agent rechnet selbst um — Tag/Woche→Monat, Tag→Minuten — und schreibt das Ergebnis fälschlich als frühere Mitarbeiter-Aussage zu; u.a. "20-25/Woche"→"90/Monat", "15-20/Tag"→"350/Monat" 2×, "3-5/Woche"→"16/Monat" 2×, "2-3 Tage"→"1200 Minuten" 2× mathematisch sogar falsch). Die übrigen 26/39 (67%) sind heterogen und NICHT durch einen Framing-Fix adressierbar: Cross-Prozess-Verwechslung (~7, Wert von Prozess A fälschlich Prozess B zugeschrieben), Fremdrede als eigene Mitarbeiter-Aussage (3, alle in einem Lauf), Wort-/Zeitpunkt-Zuschreibung (~5, "vorhin" für ein Wort das gerade erst fiel), Generalisierung/Umdeutung (~4, Häufigkeitsangabe → Statusaussage), reine Fabrikation ohne jeden Anker (2, Mitarbeiter wich explizit aus), Forced-Choice-Rest (1-2, eher KI-21- als KI-18-Territorium). **Einordnung/Entscheidung (Nutzer, 2026-07-12):** kein gezielter Punkt-Fix jetzt — die 67%-Heterogenität bestätigt die bereits dokumentierte Diagnose (diffuses Muster unter der Anker-Pflicht E3.3, nicht ein eng umreißbarer Bug), passend zu den zwei bereits gescheiterten Prompt-Fix-Versuchen. Die architektonische Frage (warum erzeugt die Anker-Pflicht so viele verschiedene Fehlklassen?) wird im Rahmen von Meilenstein 4/5 der Demo-Vorbereitungs-Roadmap (Codebase-Doku + Architektur-Funde) vertieft, nicht als weiterer Hauruck-Fix vor der Demo. Der Guard (mit dem heutigen Retry-Fix) bleibt bis dahin die primäre Absicherung.

**Architektur-Doku-Fund 2026-07-13** (Interview-Engine-Component-Deep-Dive, Meilenstein 3 der Demo-Vorbereitungs-Roadmap — kein neuer Fix-Versuch, nur ein aus der Doku-Recherche gewonnener Diagnose-Lead): beim Code-Walkthrough von [`talkerGroundingGuard.ts`](../src/services/talkerGroundingGuard.ts) fiel auf, dass `checkGroundingViolation()` einen `_traceCtx`-Parameter entgegennimmt (Zeile 160), ihn aber nie in `experimental_telemetry` des eigenen `generateText`-Calls verdrahtet (Zeile 188–194) — im Unterschied zu [`interviewTalker.ts`](../src/services/interviewTalker.ts)s `generate()`-Closure (Zeile 210), die `buildTraceMetadata` aktiv nutzt. `onTokenUsage` wird zwar bedient (Token-Zähler laufen mit), aber die Guard-Judge-Calls selbst sind im Langfuse-Trace-Baum unsichtbar. Relevanz: der Fazit-Satz des 4. Fix-Versuchs oben ("Guard-Sensitivität/Schwellenwert muss untersucht werden, nicht nur die Architektur") lässt sich ohne Trace-Daten nur über manuelle Log-Analyse verfolgen — dieser Observability-Gap ist ein konkreter, mechanisch einfacher erster Schritt, um die nächste Guard-Sensitivitäts-Untersuchung empirisch zu fundieren. Details und Vorschlag in [`docs/architecture/00-vorgeschlagene-anpassungen.md`](../docs/architecture/00-vorgeschlagene-anpassungen.md) Eintrag #7 und [`docs/architecture/04-interview-engine.md`](../docs/architecture/04-interview-engine.md) Abschnitt 4.2/7. Nicht umgesetzt — wartet auf gesonderte Freigabe für den `src/`-Edit.

**KI-18-Nebenbefund 2026-07-22 (Bookkeeping-Review, kein neuer Fix-Versuch):** die von KI-18 selbst dokumentierte strukturelle Wurzel — die Anker-Pflicht E3.3 ("Talker MUSS jede Frage an eine frühere Aussage verankern") — wurde in PROJ-46 (ADR-023) von PFLICHT zu OPTION relaxt, als Teil der Briefing-Konsolidierung, nicht als gezielter KI-18-Fix. Code-Check bestätigt: kein "ANKER"/Verankerungs-Pflicht-Text mehr in `talkerPrompt.ts`/`interviewAnalyst.ts`. PROJ-46s eigene QA maß `talker_grounding_violations`-Median 0 bei beiden Personas in QA-Runde 1 UND Runde 3 (Max sank gegenüber der vorherigen Baseline). Das ist ein starker Hinweis auf Auflösung, aber **kein Beweis** — KI-18 hat eine dokumentierte Geschichte von "Median fällt auf 0, Restmuster bleibt in Einzelfällen" (siehe 5. Fix-Versuch oben). Empfehlung: vor formaler Schließung einen gezielten KI-18-Re-Verifikationslauf fahren (analog den bisherigen 6 Versuchen), am besten im Rahmen des kommenden PROJ-42/44/46-Joint-Gates. Status bleibt bis dahin **Open**.

### KI-18 kanonischer Tabelleneintrag (Fix-Versuche 1–4 inline), archiviert 2026-07-24

| ID | Severity | Beschreibung | Entdeckt in | Fix-Aufwand |
|----|----------|-------------|-------------|-------------|
| KI-18 | 🟡 Open | `talker_grounding_violations` (KI-9-Metrik) bleibt nicht-null in den meisten Läufen beider Personas (it-support 0/1/1, vertriebler 2/1/1 über je 3 Läufe, seed 42, 2026-06-28). Wiederkehrendes Muster bei it-support: Talker schreibt der Persona einen abgeleiteten/erfundenen Wert als wörtliches Zitat zu, z.B. Turn 2 "Du hast vorhin 350 Tickets pro Monat erwähnt", obwohl die Persona zu diesem Zeitpunkt noch KEINE Zahl genannt hatte (reine Fabrikation, nicht nur eine Einheiten-Umrechnung wie beim ursprünglichen KI-9-Fall "2-3 Tage"→"1200 Minuten"). Fix-Versuch 2026-06-28: bestehende Anti-Zitat-Regel in [talkerPrompt.ts:44](../src/services/talkerPrompt.ts#L44) um ein Häufigkeits-Beispiel + Generalisierungssatz erweitert — verursachte eine `dialog_naturalness`-Regression bei it-support (Median 0.67→0.33 über 3 Läufe, Judge bemängelte Du/Sie-Inkonsistenz und unpassend formelle Floskeln, vermutlich durch Verdichtung des bereits dichten `<turn_format>`-Blocks für das lite-Modell) UND löste die Fabrikation nicht (gleicher "350 Tickets"-Fehler trat in 2/3 Läufen weiterhin auf — die Ursache ist reine Konfabulation, kein Umrechnungs-Fall, den die erweiterte Regel adressiert hätte). Fix noch am selben Tag zurückgerollt, `dialog_naturalness` danach wieder bei 0.67/3. Braucht eigenständige Diagnose, nicht über die bestehende Anti-Zitat-Regel lösbar — z.B. Frage ob der Talker überhaupt Zugriff auf einen plausiblen Ankerwert hat, wenn er "Du hast vorhin X erwähnt" formuliert. **Zweiter Fix-Versuch 2026-06-28** (justFilledSlots-Threading: quick-extract-Slots aus dem AKTUELLEN Turn bekommen im Step-Tracker/READ_ONLY_STATE/Snapshot ein eigenes Label "✓ gerade erfasst (diese Nachricht)" statt undifferenziert "✓ erfasst", plus expliziter Reminder-Block) — Hypothese war, der Talker könne "vorhin" und "diese Nachricht" nicht unterscheiden. Live-Verifikation zeigte: **gleiches Regressionsmuster wie erster Versuch.** it-support `dialog_naturalness` 0.33/3 (Gate ≥0.65 verfehlt, Judge bemängelt wieder Du/Sie-Inkonsistenz + Floskeln). `talker_grounding_violations` nicht verbessert — it-support 1/1/2 (vorher 0/1/1), vertriebler 1/2/4 (vorher 2/1/1), tendenziell schlechter. Stichprobe (it-support run1, Turn 16) zeigt zudem einen ANDEREN Fehlertyp als das ursprüngliche "vorhin"-Pattern: der Agent vermischt zwei vom Mitarbeiter genannte, aber unterschiedliche Sachverhalte (Nachbesserungsquote vs. Dauer-Einflussfaktor Datenmigration) — kein Zitat-Fabrikations-Fall, den justFilledSlots adressieren könnte. Fix zurückgerollt (Revert der Commits). Zwei unabhängige Fix-Versuche an der Talker-Prompt-Ebene sind damit gescheitert, beide mit derselben Nebenwirkung (dialog_naturalness-Regression bei it-support) — deutet auf strukturelles Problem mit Prompt-Dichte für das lite-Modell hin, nicht auf die jeweilige Regel-Formulierung. Nächster Schritt sollte vermutlich NICHT am Prompt-Text ansetzen, sondern prüfen ob Talker grundsätzlich zu viele/zu dichte Blöcke gleichzeitig sehen muss. **Root-Cause-Reanalyse 2026-06-28:** Sample der Violation-Claims über beide Personas zeigt, die meisten sind KEINE Zahlen-Fabrikation, sondern Cross-Context-Konfusion unter der Anker-Pflicht (E3.3, [PROJ-29](interview-engine/PROJ-29-gesprächsführungs-revision.md), akzeptierte + getestete Spec-Anforderung) — Talker MUSS jede Frage an eine frühere Aussage verankern, lite-Modell verwechselt/erfindet Anker wenn keiner klar ist. Beide bisherigen Fix-Versuche haben nur Verbots-Text verdichtet, ohne die Pflicht selbst anzufassen. **Dritter Fix-Versuch 2026-06-28** (architektonisch, kein Prompt-Text): Talker von Live-Streaming (`streamText`) auf Buffer-then-stream (`generateText`) umgestellt ([interviewTalker.ts](../src/services/interviewTalker.ts)) + neuer Live-Guard [talkerGroundingGuard.ts](../src/services/talkerGroundingGuard.ts) — Cross-Vendor-LLM-Judge prüft die generierte Kandidaten-Antwort gegen die echte Turn-Historie BEVOR sie den Client erreicht; bei Verletzung ein Regenerations-Versuch (gedeckelt). Begründung für Buffer-then-stream: bei Live-Token-Streaming ist der Text beim User schon sichtbar, sobald `onFinish` mit dem vollen Text läuft — ein Post-hoc-Guard kommt zu spät, ohne den Stream komplett zu puffern. Kostet Time-to-first-token (volle Generierungszeit statt sofortigem Start) + einen zusätzlichen Judge-Call pro Turn (manchmal einen weiteren Generierungs-Call bei Reparatur) — bewusster Trade-off, von Nutzer freigegeben. Call-Sites angepasst ([runInterviewTurn.ts](../src/services/runInterviewTurn.ts), [runner.ts](../src/services/__evals__/interview/runner.ts)), `tsc --noEmit` + 739/740 Unit-Tests grün. **Live-Verifikation 2026-06-28 blockiert:** erster Verifikationslauf nutzte versehentlich das falsche Modell (`INTERVIEW_MODEL`-Env statt `--models`-Flag → `gemini-3.5-flash` statt `gemini-3.1-flash-lite`, dem eigentlichen Regressions-Modell) — verworfen. Zweiter Lauf mit korrektem `--models google/gemini-3.1-flash-lite` brach mit Google-429 `RESOURCE_EXHAUSTED` (monatliches AI-Studio-Spend-Cap erschöpft, Account-Limit, kein Code-Fehler) nach Run 1 ab. Code ist fertig und eingecheckt, aber **ohne Live-Eval-Nachweis** — Nutzer-Entscheidung: Stand festhalten, Verifikation auf später verschieben (Quota-Reset oder manuelle Cap-Erhöhung). KI-18 bleibt offen bis Verifikation nachgezogen ist. **Live-Verifikation 2026-06-30** (it-support, `--runs 3 --seed 42 --store pglite`): keine Wiederholung der Fix-1/2-Regression — `dialog_naturalness` Median 0.67 (Run1 0.67, Run2 0.67, Run3 0.33), keine der drei Judge-Begründungen erwähnt Du/Sie-Inkonsistenz oder Floskel-Verdichtung. Kernproblem aber weiterhin ungelöst: `talker_grounding_violations` 1/1/0* — Run1 enthält eine echte Fabrikation ("Du hast vorhin... 3 Minuten" erwähnt, obwohl die Persona nie eine Zeitangabe machte), Run2 eine weichere Mischcharakterisierung. *Run3s Wert 0 ist nicht belastbar: Log zeigt `[talkerFactualGrounding] unexpected judge format, fallback 0` — Parser-Fallback, kein echter Befund (gleiche Scorer-Klasse wie der frühere A1a-Bug, hier erneut aufgetreten). Vollständiger Log-Grep über alle drei Läufe auf Regenerations-Events ("regenerat") ergab **0 Treffer**, obwohl der Grounding Guard 60× lief (20+17+23 Calls) und zwei echte Violations in den Output durchließ — der Live-Guard fängt diese Fehlerklasse aktuell nicht ab. Zwei Nebenbefunde, eigenständig zu verfolgen: (1) Run3 FAILed wegen eines vierfachen "Auf Wiederhören"-Wiederholungsmusters vor Abschluss, vermutlich Zusammenspiel mit KI-15s `hasUnstartedExploringStep`-Guard (Software-Installation erst nach erstem Goodbye erwähnt); (2) `talkerFactualGrounding`-Judge-Parser-Fallback erneut aufgetreten (s.o.), eigener Bug unabhängig von KI-18. **Fazit:** 3. Fix-Versuch erreicht sein Nebenziel (keine Prompt-Dichte-Regression), löst aber nicht das Kernproblem — Guard-Sensitivität/Schwellenwert muss untersucht werden, nicht nur die Architektur. KI-18 bleibt offen. Artefakte: `docs/evals/interview/2026-06-30/2026-06-30-09-{49-14,53-12,58-26}-google-gemini-3-1-flash-lite-it-support-{run1,run2,run3,aggregate}.md` **Vierter Fix-Versuch 2026-06-30** (Guard-Sensitivität statt Architektur, wie im Fazit oben gefordert): Root Cause war kein Parse-Fehler, sondern ein false-negative Judge — `talkerGroundingGuard.ts` lief 60×/3 Läufe, regenerierte aber 0×, obwohl 2 echte Violations durchliefen. Zwei Maßnahmen: (1) `GUARD_SYSTEM`-Prompt um 3 explizite Verletzungsklassen erweitert (Zahl/Wert-Fabrikation, Sachverhalt-Zuordnung, Generalisierung/Umdeutung) inkl. der zwei echten Repro-Beispiele ("3 Minuten"/"Kommt drauf an", "als Standard beschrieben"/"dreifacher Wechsel") als Few-Shot, plus "im Zweifel für Erkennung"-Regel; (2) `parseGuardResponse`-Fallback bekommt jetzt `console.warn` (analog zum bereits bekannten `talkerFactualGrounding.ts`-Pattern), damit ein künftiger Parser-Fehler nicht mehr lautlos als "keine Verletzung" durchgeht. Zusätzlich `interviewTalker.ts`: Regenerations-Loop von 1 auf `MAX_GROUNDING_REPAIRS=2` erhöht, jeder Repair wird erneut vom Guard geprüft (vorher: 1 ungeprüfter Repair-Versuch, dann stilles Ausliefern); bei Erschöpfung aller Versuche jetzt `console.error` mit `attempts`-Zähler statt Stille. 24 neue Unit-Tests (`talkerGroundingGuard.test.ts` neu, `interviewTalker.test.ts` erweitert) reproduzieren beide historischen Bug-Fälle als Fixtures, 753/754 Gesamttests grün (1 Skip vorbestehend), `tsc --noEmit` grün. **Live-Verifikation 2026-06-30** (it-support, `--models google/gemini-3.1-flash-lite --runs 3 --seed 42 --store pglite`): `talker_grounding_violations` 1/0/0 (Median 0, vorher 1/1/0*-unzuverlässig) — Log zeigt den Guard hat in diesem Lauf 4× tatsächlich regeneriert (vorher 0×/60 Calls), davon 2 exakte Treffer auf die historischen Bug-Muster ("Du hast vorhin 350 Tickets pro Monat erwähnt" bei tatsächlich genannten "75 bis 100 Tickets wöchentlich"; "als Standard beschrieben" bei tatsächlich nur "dreifacher Wechsel im Durchschnitt") — beide nach Repair sauber. Kein `[talkerGroundingGuard] unexpected judge format`-Fallback aufgetreten. Restbefund: Run1 zeigt weiterhin 1 Violation, aber ein qualitativ anderer, schwächerer Grenzfall (Wortkontinuitäts-Behauptung — Agent suggeriert, Mitarbeiter habe "unmöglich" schon einen Turn früher verwendet, tatsächlich war es eine Umformulierung) — kein erneutes Auftreten der ursprünglich dokumentierten Zahlen-Fabrikation. `dialog_naturalness` 0.67/0.67/0.33 (Median hält Gate ≥0.65, Variabilität deckungsgleich mit Baseline vom 2026-06-30-Vormittagslauf — keine neue Regression, `talkerPrompt.ts` wurde nicht angefasst). Nutzer-Entscheidung: Status bleibt **Open**, da `talker_grounding_violations` noch nicht durchgehend 0 ist — Kernmechanismus nachweislich funktionsfähig, aber kein vollständiger Fix. Artefakte: `docs/evals/interview/2026-06-30/2026-06-30-17-{24-57,30-02,30-05,34-18,34-20,39-22}-google-gemini-3-1-flash-lite-it-support-{run1,run2,run3,aggregate}.md` | Eval 2026-06-27 (Befund), 2026-06-28 (3 Fix-Versuche, 2 Rollbacks, 3. Versuch implementiert + ungetestet wg. Quota), 2026-06-30 vormittags (Live-Verifikation it-support, 3 Läufe — Regression nicht reproduziert, Kernproblem nicht gelöst), 2026-06-30 nachmittags (4. Fix-Versuch: Guard-Prompt + Parser-Logging + Repair-Loop, Live-verifiziert — deutliche Verbesserung, nicht vollständig), 2026-06-30 abends (5. Fix-Versuch: 4. Guard-Klasse + Parser-Truncation-Fix + Repair-Prompt-Umformulierung, 3× Live-verifiziert it-support — Median-Violations 1→0, Naturalness-Regression aufgetreten und behoben, neues Restmuster dokumentiert), 2026-07-11 (Modell-Spezifitäts-Klärung, siehe Fließtext unten) | offen |
